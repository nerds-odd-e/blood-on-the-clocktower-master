/** Known-bad: unconditional saved assurance (G-02 / GRIM-02). */
export function NightReadyStep() {
  return (
    <p>
      Assignments are saved. Night coaching arrives in the next update — stay
      ready at the table.
    </p>
  )
}
