---
phase: 02-setup-wizard-grimoire-capture
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/domain/bag/types.ts
  - src/domain/bag/heuristics.ts
  - src/domain/bag/validateBag.ts
  - src/domain/bag/buildBag.ts
  - src/domain/bag/buildBag.test.ts
  - src/domain/bag/index.ts
  - src/ui/setup/steps/DifficultyStep.tsx
  - src/ui/setup/steps/BagStep.tsx
  - src/ui/setup/SetupWizard.tsx
  - src/state/setupSessionStore.ts
  - e2e/setup-wizard.spec.ts
  - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
autonomous: true
requirements:
  - SETUP-02
  - SETUP-04
tags:
  - bag-builder
  - difficulty
  - vitest
  - playwright

must_haves:
  truths:
    - "Difficulty step offers Easy / Standard / Hard as discrete equal segments with default Standard and UI-SPEC help line (D-05 D-06 SETUP-02)"
    - "Segment order is Easy then Standard then Hard; exactly one selected — equal values do not merge (SETUP-02 adjacency/ordering)"
    - "Accept bag shows full private bag list grouped by team with privacy line and difficulty-tied why-this-bag note (D-09)"
    - "buildBag uses playerCount + difficulty + catalog only — player profile fields are never inputs (D-07 SETUP-04)"
    - "For every playerCount in 5..15 inclusive and each difficulty, buildBag returns a legal bag: tokens.length === N, Imp present, no Drunk draw token in tokens, Baron applies +2 outsiders/−2 townsfolk (SETUP-04 boundary)"
    - "No Regenerate control and no manual bag token edit UI (D-10 D-11)"
    - "Bag step only reachable after successful buildBag; always shows complete token set (UI Considerations E5)"
  artifacts:
    - path: "src/domain/bag/buildBag.ts"
      provides: "Pure legal TB bag generator with injectable rng"
      contains: "buildBag"
    - path: "src/domain/bag/validateBag.ts"
      provides: "Bag legality checker"
      contains: "validateBag"
    - path: "src/ui/setup/steps/BagStep.tsx"
      provides: "Private bag review UI"
      contains: "BagStep"
    - path: "src/domain/bag/buildBag.test.ts"
      provides: "Vitest matrix N×difficulty + Baron/Drunk fixtures (D-21)"
      contains: "buildBag"
  key_links:
    - from: "src/ui/setup/steps/DifficultyStep.tsx"
      to: "src/state/setupSessionStore.ts"
      via: "setDifficulty then Next builds bag"
      pattern: "difficulty"
    - from: "src/state/setupSessionStore.ts"
      to: "src/domain/bag/buildBag.ts"
      via: "buildBag({ playerCount, difficulty, catalog })"
      pattern: "buildBag"
    - from: "src/ui/setup/steps/BagStep.tsx"
      to: "BagPlan.tokens"
      via: "team-grouped chips from catalog role names"
      pattern: "setup-bag-list"
  prohibitions:
    - statement: "Must not include the Drunk character id as a physical draw token in BagPlan.tokens"
      requirement_id: SETUP-04
      category: safety
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-drunk-in-bag-tokens.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/bag-with-drunk-token.bad.json
      check_clean_fixture: tests/prohibitions/fixtures/bag-legal.clean.json
    - statement: "Must not pass player experience/age/notes into buildBag"
      requirement_id: SETUP-04
      category: values
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-profiles-into-buildBag.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/buildBag-with-profiles.bad.ts
      check_clean_fixture: src/domain/bag/types.ts
    - statement: "Must not provide Regenerate or manual bag edit controls"
      requirement_id: SETUP-04
      category: values
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-regenerate-manual-edit.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/bag-step-regenerate.bad.tsx
      check_clean_fixture: src/ui/setup/steps/BagStep.tsx
    - statement: "Must not invent illegal team mixes via difficulty (difficulty only weights legal role picks)"
      requirement_id: SETUP-04
      category: safety
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-illegal-difficulty-mix.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/illegal-difficulty-mix.bad.ts
      check_clean_fixture: src/domain/bag/buildBag.ts
    - statement: "Must not show official BotC token art — character names only"
      requirement_id: SETUP-04
      category: values
      status: resolved
      verification: judgment
  assumptions:
    - "SETUP-02 empty: difficulty always has a selection; default Standard on first visit; cannot clear to none"
    - "SETUP-04 precision: inject rng for deterministic Vitest; production uses crypto.getRandomValues-backed picker; validateBag runs before return; illegal drafts retry or throw — never paint empty bag UI"
    - "Easy/Standard/Hard heuristics follow RESEARCH recommended preference table (Claude discretion) mapped to UI-SPEC whyNote copy"
    - "REQUIREMENTS SETUP-04 text mentioning optional profiles is overridden by D-07 for bag inputs"
---

## Phase Goal

**As a** Storyteller, **I want to** walk the setup wizard, get a legal TB bag, and record who drew which role before night starts, **so that** I can reach a Night ready handoff without a paper setup sheet.

<objective>
Vertical slice: difficulty selection plus legal Trouble Brewing bag generation and private bag review (D-05–D-07, D-09–D-11, SETUP-02, SETUP-04, D-21 domain tests).

Purpose: Deliver the core SETUP-04 outcome — a legal bag the Storyteller can accept before dealing.
Output: buildBag/validateBag/heuristics + Vitest green; DifficultyStep + BagStep; E2E through Accept bag with SETUP-02 default/segment assertions.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-setup-wizard-grimoire-capture/02-02-SUMMARY.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-PATTERNS.md

<interfaces>
From plan 02 store (expected):
- wizardStep includes difficulty | bag
- players: { id, name, experience?, age?, notes? }[]
- difficulty: 'easy' | 'standard' | 'hard' (default standard)

BagPlan contract (RESEARCH):
- tokens: string[] length === playerCount — never includes the Drunk role id
- composition: { townsfolk, outsiders, minions, demons } true counts after Baron
- drunk: { coverRoleId: string } | null
- setupNotes: string[]
- whyNote: string (UI-SPEC Easy/Standard/Hard lines)

buildBag({ playerCount, difficulty, catalog, rng? }) → BagPlan
validateBag(plan, playerCount, catalog) → ok | issues

UI-SPEC CTAs: Next step (difficulty), Accept bag (bag)
Back from bag → difficulty clears bag per RESEARCH Pattern 2
</interfaces>
</context>

## Artifacts this phase produces

(Plan 03 — difficulty + legal bag slice)

- `Difficulty`, `BagPlan`, `BuildBagInput` types
- `heuristics`, `validateBag`, `buildBag`, bag barrel `index.ts`
- `buildBag.test.ts` (N=5..15 × 3 difficulties; Baron±Drunk)
- `DifficultyStep`, `BagStep` UI
- Store actions: setDifficulty, generateBag on leaving difficulty / entering bag, clearBag on Back
- E2E extended through Accept bag + SETUP-02 default/selection

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Legal bag domain + Vitest matrix</name>
  <files>src/domain/bag/types.ts, src/domain/bag/heuristics.ts, src/domain/bag/validateBag.ts, src/domain/bag/buildBag.ts, src/domain/bag/buildBag.test.ts, src/domain/bag/index.ts</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md (BagPlan, bag pipeline, Baron/Drunk pitfalls, difficulty heuristics table, rng injection — Open Questions RESOLVED Q1)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md (D-07 D-09 D-10 D-11)
    - src/domain/script/loadCatalog.ts
    - src/domain/script/schemas.ts
    - src/data/scripts/trouble-brewing/setup-chart.json
  </read_first>
  <behavior>
    - For each N in 5..15 and each difficulty easy|standard|hard: validateBag(buildBag(...)) passes
    - tokens length equals N; Imp always present; Drunk role id absent from tokens when Drunk selected — cover townsfolk id present instead with drunk.coverRoleId set
    - When Baron selected: outsider count +2 and townsfolk −2 vs base chart before fills
    - buildBag function signature accepts only playerCount, difficulty, catalog, optional rng — no profile parameter
  </behavior>
  <action>
    Implement pure domain modules under src/domain/bag/ following RESEARCH algorithm: chart lookup → difficulty-weighted role picks (heuristics.ts per RESEARCH Easy/Standard/Hard preference table) → Baron mutation → Drunk cover substitution → validateBag → whyNote from UI-SPEC copy table.

    Inject rng for tests per RESEARCH Open Questions RESOLVED Q1; production default uses crypto.getRandomValues. Export from index.ts. Write buildBag.test.ts covering the full N×difficulty matrix plus explicit Baron and Drunk fixtures (D-21). Assert validateBag invariants including absence of Drunk as a draw token via test expectations on tokens arrays (do not rely on source-comment greps).
  </action>
  <acceptance_criteria>
    - buildBag and validateBag exports exist
    - buildBag.test.ts covers player counts 5 through 15 and three difficulties
    - CURSOR_DEV=true nix develop -c npx vitest run src/domain/bag exits 0
    - buildBag TypeScript signature has no profile/experience/age/notes parameter
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npx vitest run src/domain/bag</automated>
  </verify>
  <done>Legal bag generator proven by Vitest for all TB table sizes and difficulties.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Difficulty + bag review UI wired to buildBag</name>
  <files>src/ui/setup/steps/DifficultyStep.tsx, src/ui/setup/steps/BagStep.tsx, src/state/setupSessionStore.ts, src/ui/setup/SetupWizard.tsx, e2e/setup-wizard.spec.ts, .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md (Difficulty + Bag copy, team badges, privacy line, whyNote variants)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-PATTERNS.md (BagStep / RoleRoster analog)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md (D-05–D-07 D-09–D-11)
    - src/ui/home/ScriptHome.tsx (TEAM_BADGE_CLASS / team grouping)
    - src/state/setupSessionStore.ts
  </read_first>
  <behavior>
    - On entering difficulty, Standard is selected by default (SETUP-02 D-06)
    - Selecting Easy or Hard updates session difficulty; selected segment shows accent fill; Next step calls buildBag and advances to bag
    - Bag step shows privacy line, team-grouped token names, setup notes, whyNote; Accept bag advances to deal
    - No control labeled Regenerate; no add/remove/swap token controls
    - Back from bag to difficulty clears stored bag
  </behavior>
  <action>
    Flesh DifficultyStep to UI-SPEC (segmented Easy/Standard/Hard, selected accent fill + dark label text, help line). Default selection Standard on first visit (D-06 SETUP-02). On Next from difficulty: call loadCatalog + buildBag with playerCount=players.length and session difficulty only (D-07); store BagPlan; set wizardStep bag.

    Implement BagStep: private list data-testid setup-bag-list; group tokens by team using catalog names; never list Drunk as a draw chip — show cover townsfolk + muted setup note when drunk metadata present (D-09). whyNote from BagPlan. Accept bag → deal step. Omit regenerate and manual edit controls (D-10 D-11).

    Extend e2e/setup-wizard.spec.ts through Accept bag: assert Difficulty heading; assert Standard is selected by default before any segment click; click Easy then Hard and assert selection updates; assert bag list visible, privacy line copy, Accept bag enabled (SETUP-02 + SETUP-04). Update VALIDATION.md task rows for SETUP-02/SETUP-04 to plan 03 / wave 3.
  </action>
  <acceptance_criteria>
    - DifficultyStep and BagStep components exist and are switched from SetupWizard
    - e2e/setup-wizard.spec.ts asserts Accept bag and bag privacy line
    - e2e/setup-wizard.spec.ts asserts Standard is the default selected difficulty segment on first Difficulty visit
    - e2e/setup-wizard.spec.ts asserts Easy and Hard segments are selectable (selection changes after click)
    - CURSOR_DEV=true nix develop -c npx vitest run src/domain/bag exits 0
    - CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts</automated>
  </verify>
  <done>Storyteller can set difficulty (default Standard, Easy/Hard selectable) and accept a legal private bag; domain tests + wizard E2E green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog JSON → buildBag | Bundled trusted data; still validate chart row exists for N |
| BagPlan → UI | ST-private screen; shoulder-surf is operational not network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | buildBag / validateBag | high | mitigate | Always validateBag before returning/storing; Vitest matrix blocks illegal mixes |
| T-02-05 | Elevation of Privilege | heuristics | medium | mitigate | Difficulty only reweights legal picks; cannot change demon count or invent second Imp |
| T-02-06 | Information Disclosure | BagStep | low | accept | Privacy line required; single-operator tool; no share-to-players mode (manual table check) |
| T-02-07 | Denial of Service | rng retry loop | low | mitigate | Cap retries; fail closed to error rather than hang if validate never passes |
</threat_model>

<verification>
CURSOR_DEV=true nix develop -c npx vitest run src/domain/bag
CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts
</verification>

<success_criteria>
- Difficulty UI matches D-05–D-06 with SETUP-02 E2E default + segment selection
- Legal bags for all N∈[5,15] × difficulties
- Bag review matches D-09–D-11 with D-07 profile isolation
- E2E through Accept bag green
</success_criteria>

<output>
Create `.planning/phases/02-setup-wizard-grimoire-capture/02-03-SUMMARY.md` when done
</output>
