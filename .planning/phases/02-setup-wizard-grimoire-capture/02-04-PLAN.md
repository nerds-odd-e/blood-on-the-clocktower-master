---
phase: 02-setup-wizard-grimoire-capture
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - src/domain/grimoire/types.ts
  - src/domain/grimoire/validateAssignments.ts
  - src/domain/grimoire/validateAssignments.test.ts
  - src/domain/grimoire/index.ts
  - src/ui/setup/steps/DealStep.tsx
  - src/ui/setup/steps/RecordStep.tsx
  - src/ui/setup/components/RolePicker.tsx
  - src/state/setupSessionStore.ts
  - src/ui/setup/SetupWizard.tsx
  - e2e/setup-record.spec.ts
  - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
autonomous: true
requirements:
  - GRIM-01
  - SETUP-05
tags:
  - deal
  - record
  - grimoire
  - playwright

must_haves:
  truths:
    - "Deal step shows coaching card only then Continue to record — no in-app deal or token checklist (D-12 D-19)"
    - "Record step lists players in seat order; tap player opens role picker of remaining bag tokens only (D-13 GRIM-01)"
    - "Assigning a token removes it from the pool; Clear role returns it (D-14)"
    - "When assigning the Drunk cover townsfolk token, session stores trueRoleId drunk and believedRoleId cover (RESEARCH Pattern 3)"
    - "App does not auto-assign characters to named players (D-19)"
    - "All-unassigned state shows Unassigned hint per row; empty remaining pool shows Role picker empty pool copy (GRIM-01 empty UI Considerations E7)"
    - "Wizard order remains script→players→difficulty→bag→deal→record with no skip-ahead controls (D-18 SETUP-05)"
  artifacts:
    - path: "src/domain/grimoire/validateAssignments.ts"
      provides: "AssignmentIssue[] validator for soft gate consumers"
      contains: "validateAssignments"
    - path: "src/ui/setup/steps/DealStep.tsx"
      provides: "Physical deal coaching card"
      contains: "DealStep"
    - path: "src/ui/setup/steps/RecordStep.tsx"
      provides: "Tap player → pick remaining character"
      contains: "RecordStep"
    - path: "e2e/setup-record.spec.ts"
      provides: "Playwright assign + clear remaining-pool proof"
      contains: "record"
  key_links:
    - from: "src/ui/setup/components/RolePicker.tsx"
      to: "BagPlan.tokens − assignments"
      via: "remaining multiset filter"
      pattern: "remaining"
    - from: "src/ui/setup/steps/RecordStep.tsx"
      to: "src/state/setupSessionStore.ts"
      via: "assignRole / clearRole actions"
      pattern: "assignRole"
    - from: "e2e/setup-record.spec.ts"
      to: "real preview bag chips"
      via: "assign via visible role names (D-21)"
      pattern: "Record roles"
  prohibitions:
    - statement: "Must not auto-assign bag tokens to players"
      requirement_id: GRIM-01
      category: values
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-auto-assign-record.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/auto-assign-record.bad.tsx
      check_clean_fixture: src/ui/setup/steps/RecordStep.tsx
    - statement: "Must not offer characters that are not remaining in the bag token multiset"
      requirement_id: GRIM-01
      category: safety
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-non-remaining-roles.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/non-remaining-roles.bad.tsx
      check_clean_fixture: src/ui/setup/components/RolePicker.tsx
    - statement: "Must not use a Vaul/sheet library for the role picker — inline panel only"
      requirement_id: GRIM-01
      category: values
      status: resolved
      verification: judgment
  assumptions:
    - "GRIM-01 encoding: bagRoleId / role ids are catalog string ids; display uses catalog name strings"
    - "GRIM-01 empty: zero assignments is a valid intermediate state; Start night soft-gate handled in plan 05"
    - "Assignment record shape includes bagRoleId plus optional trueRoleId/believedRoleId for Drunk cover"
---

## Phase Goal

**As a** Storyteller, **I want to** walk the setup wizard, get a legal TB bag, and record who drew which role before night starts, **so that** I can reach a Night ready handoff without a paper setup sheet.

<objective>
Vertical slice: deal coaching plus grimoire role recording with remaining-token pool (D-12–D-14, D-19, GRIM-01, SETUP-05 deal/record steps).

Purpose: Capture who drew what after the physical random deal.
Output: DealStep, RecordStep, RolePicker, validateAssignments + unit tests, setup-record E2E.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-setup-wizard-grimoire-capture/02-03-SUMMARY.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-PATTERNS.md

<interfaces>
From plan 03:
- session.bag: BagPlan with tokens[], drunk?
- Accept bag already advances toward deal

AssignmentIssue codes (RESEARCH / UI-SPEC):
- unassigned { playerId }
- token_mismatch { detail }
- duplicate_token { roleId }

validateAssignments(session) → AssignmentIssue[] (empty = clean) — do not throw

Store actions to add:
- assignRole(playerId, bagRoleId)
- clearRole(playerId)
- remainingTokens derived helper

UI-SPEC: Continue to record, Record roles, Pick character, Clear role, Unassigned hint
</interfaces>
</context>

## Artifacts this phase produces

(Plan 04 — deal + record slice)

- `Assignment`, `AssignmentIssue` types
- `validateAssignments` + `validateAssignments.test.ts`
- `DealStep`, `RecordStep`, `RolePicker`
- Store assign/clear + remaining pool
- `e2e/setup-record.spec.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validateAssignments domain + RED record E2E</name>
  <files>src/domain/grimoire/types.ts, src/domain/grimoire/validateAssignments.ts, src/domain/grimoire/validateAssignments.test.ts, src/domain/grimoire/index.ts, e2e/setup-record.spec.ts</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md (Pattern 3 remaining-token pool, AssignmentIssue, Drunk cover model)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md (Record + soft-gate issue copy — validator codes must align)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
  </read_first>
  <behavior>
    - validateAssignments: clean when multiset(assignments.bagRoleId) equals multiset(bag.tokens) and every player assigned once
    - reports unassigned for missing players; duplicate_token when same physical token id assigned twice; token_mismatch when multisets differ
    - E2E RED: walk wizard to record, assign all players from remaining chips, assert each row shows a character name; clear one role and assert chip returns
  </behavior>
  <action>
    Implement grimoire domain types and validateAssignments returning AssignmentIssue[] (never throws). Cover match / missing / duplicate cases in validateAssignments.test.ts.

    Create e2e/setup-record.spec.ts (D-21) that drives through deal into Record roles using real preview UI (reuse helpers or inline flow from setup-wizard). Assert assign-all path and clear restores a token to the picker. Spec should fail until Task 2 UI exists.
  </action>
  <acceptance_criteria>
    - validateAssignments export exists
    - CURSOR_DEV=true nix develop -c npx vitest run src/domain/grimoire exits 0
    - e2e/setup-record.spec.ts exists referencing Record roles and Clear role
    - CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-record.spec.ts exits non-zero before Task 2 (RED)
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npx vitest run src/domain/grimoire</automated>
  </verify>
  <done>Assignment validator green; record E2E written and failing pending UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deal + Record UI with remaining-token picker</name>
  <files>src/ui/setup/steps/DealStep.tsx, src/ui/setup/steps/RecordStep.tsx, src/ui/setup/components/RolePicker.tsx, src/state/setupSessionStore.ts, src/ui/setup/SetupWizard.tsx, e2e/setup-record.spec.ts, .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md (Deal + Record layout, picker panel, copy)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md (D-12 D-13 D-14 D-19)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-PATTERNS.md (RolePicker / RecordStep)
    - src/state/setupSessionStore.ts
  </read_first>
  <behavior>
    - Deal: coaching paragraph + Continue to record only
    - Record: tap unassigned → picker remaining tokens; tap assigned → picker + Clear role
    - Start night button may exist but soft-gate behavior is plan 05 — this plan greens assign/clear E2E only
  </behavior>
  <action>
    Implement DealStep per UI-SPEC (D-12). Implement RecordStep + RolePicker as full-width inline panel under the list (not a drawer library). Picker lists only remaining bag tokens grouped by team chips (D-13). assignRole/clearRole in the store update assignments and Drunk cover metadata when the assigned bagRoleId equals bag.drunk.coverRoleId (trueRoleId drunk, believedRoleId cover). Clear returns token to pool (D-14). Never auto-assign on deal continue (D-19).

    Expose data-testid hooks (setup-player-row, setup-role-chip) for Playwright. Green e2e/setup-record.spec.ts. Update VALIDATION.md GRIM-01 row to plan 04 / wave 4.
  </action>
  <acceptance_criteria>
    - DealStep and RecordStep mounted in SetupWizard switch
    - RolePicker filters to remaining tokens only
    - CURSOR_DEV=true nix develop -c npx vitest run src/domain/grimoire exits 0
    - CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-record.spec.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-record.spec.ts</automated>
  </verify>
  <done>Storyteller can coach the physical deal and record assignments via remaining-token picker with clear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ST assignment clicks → grimoire state | Local trusted operator input |
| BagPlan tokens → picker options | Must not leak non-bag roles into picker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | RolePicker pool | high | mitigate | Derive options strictly from remaining multiset of bag.tokens; unit + E2E prove clear restores |
| T-02-09 | Tampering | validateAssignments | medium | mitigate | Multiset equality checks; structured issue codes for soft gate |
| T-02-10 | Information Disclosure | Record step | low | accept | Roles visible only on ST device; same posture as bag step |
| T-02-11 | Repudiation | assignments map | low | accept | Local-only tool; no audit server required at ASVS L1 |
</threat_model>

<verification>
CURSOR_DEV=true nix develop -c npx vitest run src/domain/grimoire
CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-record.spec.ts
</verification>

<success_criteria>
- Deal coaching card shipped (D-12)
- Remaining-token recording with clear (D-13 D-14)
- No auto-assign (D-19)
- setup-record E2E green
</success_criteria>

<output>
Create `.planning/phases/02-setup-wizard-grimoire-capture/02-04-SUMMARY.md` when done
</output>
