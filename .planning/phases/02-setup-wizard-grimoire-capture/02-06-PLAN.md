---
phase: 02-setup-wizard-grimoire-capture
plan: 06
type: execute
wave: 6
depends_on:
  - "02-05"
files_modified:
  - src/state/setupSessionStore.ts
  - src/state/setupSessionSemantics.ts
  - src/state/setupSessionSemantics.test.ts
  - src/state/setupSessionStore.assignRole.test.ts
  - src/state/idbStorage.ts
  - src/state/persistStatus.ts
  - src/ui/setup/SetupWizard.tsx
  - src/ui/setup/steps/NightReadyStep.tsx
  - src/ui/setup/steps/RecordStep.tsx
  - src/ui/setup/components/ConfirmDialog.tsx
  - e2e/setup-wizard.spec.ts
  - e2e/setup-record.spec.ts
  - tests/prohibitions/
  - .planning/phases/02-setup-wizard-grimoire-capture/02-02-PLAN.md
  - .planning/phases/02-setup-wizard-grimoire-capture/02-03-PLAN.md
  - .planning/phases/02-setup-wizard-grimoire-capture/02-04-PLAN.md
  - .planning/phases/02-setup-wizard-grimoire-capture/02-05-PLAN.md
  - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
autonomous: true
gap_closure: true
requirements:
  - SETUP-05
  - GRIM-01
  - GRIM-02
  - SETUP-04
tags:
  - gap-closure
  - persistence
  - hydration
  - prohibitions

must_haves:
  truths:
    - "Shape-valid but semantically impossible persisted sessions (wizardStep bag/deal/record/nightReady with bag null, or roster/bag cardinality mismatch) reset to a fresh wizard and show the persist-hydrate recovery copy instead of a blank /setup (G-01, SETUP-05 empty/null session edge)"
    - "Night ready shows Assignments are saved only after the latest critical IndexedDB write for that session revision succeeds; saving and error/retry states are visible when durability is pending or failed (G-02)"
    - "Assigning a Drunk cover token persists trueRoleId drunk and believedRoleId equal to the cover bagRoleId (behavior_unverified closure)"
    - "ConfirmDialog restores focus to the previously focused trigger element after dismiss, confirm, Escape, or backdrop close (UI-SPEC dialog a11y / WR-01)"
    - "All Phase 2 test-tier must_haves.prohibitions carry check_kind, check_target, and check_violation_fixture (or are re-authored to judgment with an explicit reason), so prohibition-enforcement can green (G-03)"
    - statement: "Role picker with up to 15 rows scrolls vertically without horizontal overflow at 390×844"
      verification: backstop
    - statement: "Soft-gate dialog with many issues scrolls inside the dialog"
      verification: backstop
  artifacts:
    - path: "src/state/setupSessionSemantics.ts"
      provides: "Cross-field semantic validation for hydrated setup sessions"
      contains: "assertSetupSessionSemantics"
    - path: "src/state/setupSessionStore.ts"
      provides: "merge calls semantic validation; persistWriteStatus + awaitable critical save"
      contains: "persistWriteStatus"
    - path: "src/ui/setup/steps/NightReadyStep.tsx"
      provides: "Gated saved assurance with saving/error/retry"
      contains: "Assignments are saved"
    - path: "tests/prohibitions/"
      provides: "node:test prohibition guards + known-bad fixtures for G-03"
      contains: "GSD_PROHIB_SUBJECT"
    - path: "e2e/setup-wizard.spec.ts"
      provides: "Browser recovery for shape-valid impossible sessions"
      contains: "nightReady"
  key_links:
    - from: "PersistedSetupSessionSchema.safeParse success"
      to: "assertSetupSessionSemantics"
      via: "merge rejects invariant failures → freshSession + hydrationError"
      pattern: "assertSetupSessionSemantics"
    - from: "RecordStep / SetupWizard Start night → nightReady"
      to: "awaitCriticalPersist or equivalent"
      via: "persistWriteStatus saved before assurance copy"
      pattern: "persistWriteStatus"
    - from: "NightReadyStep body"
      to: "persistWriteStatus"
      via: "saved-only assurance; error shows retry"
      pattern: "Retry"
    - from: "must_haves.prohibitions check_* scalars"
      to: "tests/prohibitions/*.test.mjs + fixtures"
      via: "check prohibition-enforcement fail-first"
      pattern: "check_violation_fixture"
  prohibitions:
    - statement: "Must not show Assignments are saved while the latest critical IndexedDB write is pending or has failed"
      requirement_id: GRIM-02
      category: transparency
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-premature-saved-assurance.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/night-ready-premature-saved.bad.tsx
      check_clean_fixture: src/ui/setup/steps/NightReadyStep.tsx
    - statement: "Must not merge shape-valid impossible sessions that strand bag/deal/record/nightReady without a bag"
      requirement_id: SETUP-05
      category: safety
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-impossible-session-merge.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/session-bag-null-nightReady.bad.json
      check_clean_fixture: tests/prohibitions/fixtures/session-valid-minimal.clean.json
  assumptions:
    - "Edge probe items for SETUP-01/02/03 adjacency/ordering/encoding and SETUP-04 boundary/precision remain covered by executed plans 02-01–02-05 or human overflow backstops; this gap plan only closes persistence/recovery/null-bag session edges named in 02-VERIFICATION"
    - "Phone overflow extremes (long names, 15-role picker, many-issue dialog) stay human/backstop per UI Considerations E7 and VERIFICATION Human Verification Required"
    - "Existing PLAN task bodies in 02-01–02-05 are not rewritten; only must_haves.prohibitions check_* scalars (and judgment re-authors where needed) are patched for G-03"
---

## Phase Goal

**As a** Storyteller, **I want to** walk the setup wizard, get a legal TB bag, and record who drew which role before night starts, **so that** I can reach a Night ready handoff without a paper setup sheet.

<objective>
Gap closure (G-01, G-02, G-03): persisted session integrity — semantic hydration recovery, truthful durable-save assurance on Night ready, Drunk assignment proof, ConfirmDialog focus restore, and test-tier prohibition fixtures/descriptors.

Purpose: Unblock phase verification after 7/11 must-haves; close blocker recovery and data-loss paths without replanning 02-01–02-05.
Output: Semantic merge validation + E2E; persistWriteStatus-gated Night ready + failed-write E2E; Drunk unit test; focus restore; prohibition node:test suite + PLAN frontmatter descriptor patches.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-setup-wizard-grimoire-capture/02-VERIFICATION.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-REVIEW.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-05-SUMMARY.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md

<interfaces>
Existing store (do not remove shape Zod — extend after parse):
- PersistedSetupSessionSchema + merge in useSetupSessionStore
- hydrationError + UI-SPEC copy Couldn’t restore your setup (reuse for semantic failures)
- assignRole Drunk branch: trueRoleId drunk, believedRoleId bagRoleId when bag.drunk.coverRoleId matches
- NightReadyStep currently returns null when bag is null; body always claims saved
- idbStorage.setItem can reject; persist middleware does not surface status
- ConfirmDialog focuses dismiss on open; cleanup does not restore prior activeElement

Semantic invariants (CR-01 / G-01) after Zod success:
- Downstream steps bag|deal|record|nightReady require non-null bag that passes validateBag(plan, players.length, catalog)
- bag.tokens.length === players.length
- Downstream steps require 5–15 players with unique ids and unique trimmed nonempty names
- Every assignments record key equals assignment.playerId and references a current player id
- On any failure: freshSession() + hydrationError true (same recovery path as corrupt JSON)

Durable save (CR-02 / G-02):
- persistWriteStatus: saving | saved | error (ephemeral, not partialized)
- Critical path: advancing to nightReady awaits an explicit idb write of the partialized session (or equivalent flush) before/assurance-gated with status
- Night ready body: saved copy only when status is saved; saving copy while pending; error copy + Retry control on failure
- Claude discretion for error/saving copy — match table-lantern voice; do not invent shaming

Prohibition enforcement (G-03):
- Producer: node --test with GSD_PROHIB_SUBJECT injection for fail-first
- Patch check_* on existing 13 test-tier items in 02-02..02-05 frontmatter only
</interfaces>
</context>

## Artifacts this phase produces

(Plan 06 — Persisted Session Integrity gap closure)

- `assertSetupSessionSemantics` (export) in `src/state/setupSessionSemantics.ts`
- `persistWriteStatus` + `awaitCriticalPersist` / `retryCriticalPersist` (or equivalent names) on `useSetupSessionStore`
- Gated Night ready assurance UI (saving / saved / error+Retry)
- `ConfirmDialog` prior-focus restore on close
- Unit: Drunk cover `assignRole` transition test
- Unit: semantic validation cases (null bag on nightReady, roster/bag mismatch)
- E2E: shape-valid impossible session recovery; rejected IndexedDB write on Night ready
- `tests/prohibitions/*.test.mjs` + `tests/prohibitions/fixtures/*` known-bad (and clean) subjects
- Patched `check_kind` / `check_target` / `check_violation_fixture` (+ optional `check_clean_fixture`) on all Phase 2 test-tier prohibitions in plans 02-02–02-05
- VALIDATION.md rows for gap sampling commands

### Edge probe coverage (gap scope only)

| Probe | Status in this plan |
|-------|---------------------|
| SETUP-05 / GRIM empty-null impossible session (bag null on downstream step) | COVERED — G-01 semantic merge + E2E |
| GRIM-02 premature saved assurance | COVERED — G-02 + prohibition fixture |
| SETUP-01/02/03 adjacency, ordering, encoding; SETUP-04 boundary/precision; GRIM-01 encoding | NOT in gap scope — already exercised by executed 02-01–05 or deferred/human per VERIFICATION |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Semantic hydration recovery + Drunk assignment proof</name>
  <files>src/state/setupSessionSemantics.ts, src/state/setupSessionSemantics.test.ts, src/state/setupSessionStore.ts, src/state/setupSessionStore.assignRole.test.ts, e2e/setup-wizard.spec.ts, .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-VERIFICATION.md (G-01, behavior_unverified Drunk)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-REVIEW.md (CR-01)
    - src/state/setupSessionStore.ts (merge, PersistedSetupSessionSchema, assignRole Drunk branch)
    - src/domain/bag/validateBag.ts
    - e2e/setup-wizard.spec.ts (existing corrupt JSON recovery test)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md (Error state persist hydrate fail copy)
  </read_first>
  <behavior>
    - assertSetupSessionSemantics rejects wizardStep bag/deal/record/nightReady when bag is null
    - assertSetupSessionSemantics rejects bag.tokens.length !== players.length and bags failing validateBag
    - assertSetupSessionSemantics rejects duplicate player ids, assignment key/playerId mismatch, orphan assignment keys
    - merge after Zod success calls assertSetupSessionSemantics; failure yields freshSession + hydrationError true
    - assignRole on drunk coverRoleId yields trueRoleId drunk and believedRoleId cover
    - Playwright: seed IndexedDB with shape-valid nightReady+bag:null JSON under key st-copilot-setup-session; reload /setup shows Couldn’t restore your setup and Trouble Brewing script step (not blank)
    - Playwright: seed roster/bag length mismatch session; same recovery
  </behavior>
  <action>
    Implement pure assertSetupSessionSemantics(session, catalog) encoding CR-01 invariants (G-01, SETUP-05). Wire it inside the existing Zustand persist merge after PersistedSetupSessionSchema.safeParse succeeds — on semantic failure return freshSession + hydrationError true (same as Zod failure). Do not weaken shape Zod; do not strand BagStep/NightReadyStep on null bag.

    Add Vitest coverage for semantic rejects and for assignRole Drunk cover metadata (trueRoleId drunk, believedRoleId = cover bagRoleId) per D-13/D-14 store contract and VERIFICATION behavior_unverified item.

    Extend e2e/setup-wizard.spec.ts with two recovery cases beyond invalid JSON: (1) shape-valid nightReady with bag null; (2) shape-valid record/bag step with players.length !== bag.tokens.length. Seed via IndexedDB keyval put of JSON matching persist shape (zustand persist envelope if the app stores {state,version} — match whatever getItem currently returns). Assert recovery copy and script-step heading.

    Update 02-VALIDATION.md sampling map with these unit/E2E commands. Do not rewrite plans 02-01–02-05 task bodies.
  </action>
  <acceptance_criteria>
    - File src/state/setupSessionSemantics.ts exports assertSetupSessionSemantics
    - setupSessionStore merge invokes assertSetupSessionSemantics after successful Zod parse
    - src/state/setupSessionSemantics.test.ts covers null-bag downstream step and roster/bag mismatch
    - src/state/setupSessionStore.assignRole.test.ts (or equivalent) asserts Drunk cover trueRoleId/believedRoleId
    - e2e/setup-wizard.spec.ts includes a test that seeds bag:null nightReady and expects Couldn’t restore your setup
    - CURSOR_DEV=true nix develop -c npm run test:unit exits 0
    - CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npm run test:unit && CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts</automated>
  </verify>
  <done>Impossible persisted sessions recover to a fresh wizard; Drunk assignment branch is unit-proven (G-01 + behavior_unverified).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Durable Night ready save + focus restore</name>
  <files>src/state/idbStorage.ts, src/state/persistStatus.ts, src/state/setupSessionStore.ts, src/ui/setup/SetupWizard.tsx, src/ui/setup/steps/RecordStep.tsx, src/ui/setup/steps/NightReadyStep.tsx, src/ui/setup/components/ConfirmDialog.tsx, e2e/setup-record.spec.ts, .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-VERIFICATION.md (G-02)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-REVIEW.md (CR-02, WR-01)
    - src/state/idbStorage.ts
    - src/state/setupSessionStore.ts
    - src/ui/setup/steps/NightReadyStep.tsx
    - src/ui/setup/SetupWizard.tsx (onStartNight → setWizardStep nightReady)
    - src/ui/setup/components/ConfirmDialog.tsx
    - .planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md (Night ready body; dialog focus trap)
    - e2e/setup-record.spec.ts
  </read_first>
  <behavior>
    - Advancing to nightReady sets persistWriteStatus saving, awaits critical IndexedDB write of partialized session, then saved; on reject sets error without claiming saved
    - NightReadyStep renders Assignments are saved only when persistWriteStatus is saved
    - On error, Night ready (or blocking overlay on that step) shows non-shaming error copy and a Retry control that re-invokes the critical persist
    - Playwright can inject a failing setItem seam (page.evaluate monkeypatch on idb-keyval path, or test-only hook on idbStorage) so Start night / nightReady path shows error and never the saved assurance until a successful retry
    - ConfirmDialog captures the focused element before moving focus into the dialog and restores it on unmount when still connected
  </behavior>
  <action>
    Close G-02 per CR-02: expose persistWriteStatus (saving|saved|error) as ephemeral store state (omit from partialize). Implement awaitCriticalPersist that serializes the same fields partialize already persists under name st-copilot-setup-session and awaits idbStorage.setItem (or a thin wrapper that records status). Wire SetupWizard/RecordStep Start night and soft-gate Start anyway so nightReady transition either awaits this flush or lands on Night ready with status-driven copy — never unconditional saved text.

    Update NightReadyStep: gate the UI-SPEC saved sentence on persistWriteStatus === saved; show a saving line while saving; on error show retry (Claude discretion copy, table-lantern, no shame). Ensure BagStep/NightReadyStep null-bag path remains unreachable after Task 1 semantics.

    Close WR-01: ConfirmDialog restores focus to the pre-open activeElement (or provided trigger) after confirm, dismiss, Escape, and backdrop close.

    Extend e2e/setup-record.spec.ts for failed-write then retry-success. Keep URL on /setup. Update VALIDATION.md.
  </action>
  <acceptance_criteria>
    - useSetupSessionStore state includes persistWriteStatus with values saving, saved, and error
    - NightReadyStep source does not render the saved assurance unless persistWriteStatus is saved (branch or conditional)
    - Retry control exists for persistWriteStatus error
    - ConfirmDialog effect cleanup restores focus to a previously captured Element when document.contains(that element)
    - e2e/setup-record.spec.ts covers a rejecting write path that withholds the saved assurance
    - CURSOR_DEV=true nix develop -c npm run test:unit exits 0
    - CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-record.spec.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npm run test:unit && CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-record.spec.ts</automated>
  </verify>
  <done>Assignments are saved only after durable write success; dialog focus returns to trigger (G-02 + WR-01).</done>
</task>

<task type="auto">
  <name>Task 3: Test-tier prohibition fixtures and descriptors (G-03)</name>
  <files>tests/prohibitions/, .planning/phases/02-setup-wizard-grimoire-capture/02-02-PLAN.md, .planning/phases/02-setup-wizard-grimoire-capture/02-03-PLAN.md, .planning/phases/02-setup-wizard-grimoire-capture/02-04-PLAN.md, .planning/phases/02-setup-wizard-grimoire-capture/02-05-PLAN.md, .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md, package.json</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-VERIFICATION.md (G-03 Prohibition Accounting)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-02-PLAN.md (must_haves.prohibitions)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-03-PLAN.md (must_haves.prohibitions)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-04-PLAN.md (must_haves.prohibitions)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-05-PLAN.md (must_haves.prohibitions)
    - $HOME/.cursor/gsd-core/references/prohibition-probe.md (check_* scalars, GSD_PROHIB_SUBJECT, node-test fail-first)
  </read_first>
  <action>
    Close G-03 for all 13 existing test-tier prohibitions across plans 02-02–02-05 (count statements with verification test in those four files). For each item either:

    A) Wire a full descriptor: check_kind node-test, check_target pointing at a new tests/prohibitions/*.test.mjs file, check_violation_fixture pointing at a known-bad subject under tests/prohibitions/fixtures/, and preferably check_clean_fixture pointing at the real production file or a clean fixture; OR
    B) Re-author verification to judgment with a one-line reason that Playwright/human already covers it and node-test is not the supported tier — only when a mechanical node:test subject is genuinely ambiguous.

    Prefer A for domain and source-pattern guards (Drunk token in bag.tokens, profiles passed into buildBag, Regenerate/manual edit strings, /play navigation from Night ready, hard-disabled Start night, drag-and-drop/sheet imports, wizard step path segments, auto-assign, remaining-token violations, silent bypass without validateAssignments, plus this plan’s premature-saved and impossible-merge guards).

    Each node:test file must: (1) without GSD_PROHIB_SUBJECT, pass non-vacuously against the clean subject/production file; (2) with GSD_PROHIB_SUBJECT set to the bad fixture path, fail with a named test distinct from the filename (fail-first). Use node:test and node:assert — not Vitest — because prohibition-enforcement spawns node --test.

    Patch ONLY the prohibitions YAML blocks in 02-02–02-05 PLAN.md frontmatter — do not alter those plans’ task XML bodies or re-execute them. Include this plan’s two new prohibitions (already declared above) in the suite. Optionally add package.json script test:prohibitions running node --test tests/prohibitions. Update VALIDATION.md with the prohibition sampling command.
  </action>
  <acceptance_criteria>
    - Directory tests/prohibitions/ contains at least one .test.mjs and at least one fixtures/ known-bad file
    - Every verification: test prohibition in 02-02, 02-03, 02-04, and 02-05 PLAN.md frontmatter includes a non-empty check_violation_fixture string (or was changed to verification: judgment with a reason note in assumptions or statement)
    - CURSOR_DEV=true nix develop -c node --test tests/prohibitions exits 0 when run without GSD_PROHIB_SUBJECT
    - For at least one wired guard, running with GSD_PROHIB_SUBJECT pointing at its bad fixture yields a failing test (manual spot-check during execute; document in SUMMARY)
    - 02-01–02-05 task XML bodies unchanged (diff limited to prohibitions frontmatter + new tests/ files + VALIDATION/package.json)
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c node --test tests/prohibitions</automated>
  </verify>
  <done>Test-tier prohibitions are mechanically enforceable with known-bad fixtures (G-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB → Zustand merge | Untrusted persisted JSON (shape + semantics) enters live session |
| Store → IndexedDB setItem | Critical grimoire durability crossing async storage |
| Night ready UI ← persistWriteStatus | Operator-facing assurance must match durable truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Tampering | setupSessionStore merge | high | mitigate | assertSetupSessionSemantics after Zod; impossible sessions reset + hydrationError (G-01); E2E seeds bag-null nightReady |
| T-02-17 | Tampering | awaitCriticalPersist / idbStorage.setItem | high | mitigate | Surface persistWriteStatus error; withhold saved assurance until write resolves; Retry path (G-02); E2E rejecting seam |
| T-02-18 | Information Disclosure | Night ready summary | low | accept | ST-only local device; summary shows counts not full private bag list |
| T-02-19 | Repudiation | premature saved copy | medium | mitigate | Gate Assignments are saved on persistWriteStatus saved; prohibition fixture no-premature-saved-assurance |
| T-02-20 | Elevation of Privilege | soft-gate Start anyway | medium | mitigate | Unchanged D-15 issue list + confirm; this plan does not weaken validateAssignments |
| T-02-SC | Tampering | npm installs | high | mitigate | No new runtime packages in this plan; if any added, require RESEARCH legitimacy + human confirm for ASSUMED/SUS |
</threat_model>

<verification>
CURSOR_DEV=true nix develop -c npm run test:unit
CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts e2e/setup-record.spec.ts
CURSOR_DEV=true nix develop -c node --test tests/prohibitions
CURSOR_DEV=true nix develop -c npm run build
</verification>

<success_criteria>
- G-01 closed: shape-valid impossible sessions recover with hydrate error copy (no blank /setup)
- G-02 closed: saved assurance only after durable write success; error+retry when write rejects
- G-03 closed: Phase 2 test-tier prohibitions have check_violation_fixture descriptors (or explicit judgment re-author)
- Drunk cover assignment unit-proven
- ConfirmDialog restores focus after close
- Unit + targeted Playwright + prohibition node:test green
</success_criteria>

<output>
Create `.planning/phases/02-setup-wizard-grimoire-capture/02-06-SUMMARY.md` when done
</output>
