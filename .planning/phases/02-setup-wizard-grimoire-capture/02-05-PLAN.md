---
phase: 02-setup-wizard-grimoire-capture
plan: 05
type: execute
wave: 5
depends_on:
  - "02-04"
files_modified:
  - src/ui/setup/steps/NightReadyStep.tsx
  - src/ui/setup/steps/RecordStep.tsx
  - src/ui/setup/components/ConfirmDialog.tsx
  - src/state/setupSessionStore.ts
  - src/ui/setup/SetupWizard.tsx
  - e2e/setup-record.spec.ts
  - e2e/setup-wizard.spec.ts
  - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
autonomous: true
requirements:
  - GRIM-02
  - SETUP-05
  - GRIM-01
tags:
  - night-ready
  - soft-gate
  - playwright

must_haves:
  truths:
    - "Start night is always tappable on the record step; when validateAssignments fails, a dialog lists concrete issues then requires Start anyway or Keep recording (D-15 GRIM-02)"
    - "Clean validation or confirmed Start anyway advances to Night ready summary on /setup — URL stays /setup and /play is not opened (D-17)"
    - "Night ready shows Display title Night ready, summary labels Players · Difficulty · Bag · Assignments, and muted coaching-comes-next body with no primary CTA to /play (D-17 UI-SPEC)"
    - "Soft-gate Start anyway uses secondary outline styling — not destructive red (UI-SPEC)"
    - "No quit-before-start / dropout re-bag feature control exists (D-16)"
    - "Full wizard order script→players→difficulty→bag→deal→record→nightReady is enforceable end-to-end (D-18 SETUP-05)"
    - "Incomplete recording still surfaces unassigned/mismatch/duplicate copy before override — never silent bypass (GRIM-02 unclassified → D-15)"
    - statement: "Long player names wrap in roster rows at 390×844 without horizontal scroll"
      verification: backstop
    - statement: "Role picker with up to 15 rows scrolls vertically without horizontal overflow"
      verification: backstop
    - statement: "Soft-gate dialog with many issues scrolls inside the dialog"
      verification: backstop
  artifacts:
    - path: "src/ui/setup/steps/NightReadyStep.tsx"
      provides: "Night ready handoff summary for Phase 3"
      contains: "NightReadyStep"
    - path: "src/ui/setup/steps/RecordStep.tsx"
      provides: "Soft Start night gate wired to validateAssignments"
      contains: "Start night"
    - path: "e2e/setup-record.spec.ts"
      provides: "Soft gate + Night ready Playwright proof"
      contains: "Start anyway"
  key_links:
    - from: "RecordStep Start night"
      to: "validateAssignments"
      via: "issues.length ? open soft-gate dialog : nightReady"
      pattern: "validateAssignments"
    - from: "ConfirmDialog soft-gate"
      to: "wizardStep nightReady"
      via: "Start anyway confirm"
      pattern: "Start anyway"
    - from: "NightReadyStep"
      to: "/setup route"
      via: "no navigate to /play"
      pattern: "Night ready"
  prohibitions:
    - statement: "Must not silently bypass incomplete recording without listing issues and requiring confirm"
      requirement_id: GRIM-02
      category: transparency
      status: resolved
      verification: test
    - statement: "Must not navigate to /play after Start night confirmation"
      requirement_id: GRIM-02
      category: values
      status: resolved
      verification: test
    - statement: "Must not hard-disable Start night as the only gate (soft confirm-to-override required)"
      requirement_id: GRIM-02
      category: values
      status: resolved
      verification: test
    - statement: "Must not ship a quit-before-start dropout re-bag flow"
      requirement_id: SETUP-05
      category: values
      status: resolved
      verification: judgment
    - statement: "Must not use shaming or guilt framing in soft-gate or Night ready copy"
      requirement_id: GRIM-02
      category: values
      status: resolved
      verification: judgment
  assumptions:
    - "GRIM-02 unclassified: soft confirm-to-override per D-15 is the phase resolution of REQUIREMENTS block language"
    - "Back from Night ready returns to record (UI-SPEC recommendation)"
    - "UI Considerations backstop truths above are held out for visual verify at /gsd-verify-work"
---

## Phase Goal

**As a** Storyteller, **I want to** walk the setup wizard, get a legal TB bag, and record who drew which role before night starts, **so that** I can reach a Night ready handoff without a paper setup sheet.

<objective>
Vertical slice: soft Start night gate plus Night ready handoff staying on /setup (D-15–D-17, GRIM-02), closing the phase goal.

Purpose: Let Storyteller proceed to night readiness with explicit mismatch warnings, without shipping the Phase 3 coach.
Output: Soft-gate dialog, NightReadyStep, full-path E2E green, VALIDATION map updated.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-setup-wizard-grimoire-capture/02-04-SUMMARY.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-PATTERNS.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md

<interfaces>
From plan 04:
- validateAssignments(session) → AssignmentIssue[]
- RecordStep with Start night control present

Soft-gate copy (UI-SPEC):
- Heading: Recording is incomplete
- Body prefix: You can still start night, but fix these if you can:
- Issue templates for unassigned / mismatch / duplicate
- CTAs: Start anyway (secondary outline — not destructive red, not primary accent fill) / Keep recording (dismiss)
- Escape → Keep recording; role=dialog + focus trap

Night ready:
- text-display title Night ready
- Summary: player count, difficulty name, composition counts, Assignments recorded
- No link/button to /play
- Persist session for Phase 3
</interfaces>
</context>

## Artifacts this phase produces

(Plan 05 — soft gate + Night ready slice)

- Soft-gate ConfirmDialog variant on RecordStep
- `NightReadyStep`
- Store transition to `wizardStep: 'nightReady'`
- E2E: incomplete → warning → Start anyway → Night ready; clean path → Night ready; URL remains /setup
- VALIDATION.md sampling map filled for all SETUP/GRIM rows

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Soft Start night gate + Night ready summary</name>
  <files>src/ui/setup/steps/RecordStep.tsx, src/ui/setup/steps/NightReadyStep.tsx, src/ui/setup/components/ConfirmDialog.tsx, src/state/setupSessionStore.ts, src/ui/setup/SetupWizard.tsx</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-PATTERNS.md (NightReadyStep analog: SetupStub + ScriptHome empty/error cards)
    - src/ui/setup/SetupStub.tsx
    - src/ui/home/ScriptHome.tsx
    - .planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md (D-15 D-16 D-17)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md (soft-gate + Night ready copy, dialog a11y, secondary-outline Start anyway rule)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md (Pattern 4 Soft Night Gate, Pitfall 5)
    - src/domain/grimoire/validateAssignments.ts
    - src/ui/setup/steps/RecordStep.tsx
  </read_first>
  <behavior>
    - Start night always enabled on record step
    - Issues present → dialog lists mapped UI-SPEC issue strings; Start anyway → nightReady; Keep recording / Escape → stay on record
    - Start anyway uses secondary outline styling (not destructive red, not primary accent fill)
    - Issues empty → nightReady immediately
    - Night ready has no control navigating to /play; URL path remains /setup
    - No quit-before-start control appears anywhere in wizard chrome (D-16)
  </behavior>
  <action>
    Wire RecordStep Start night to validateAssignments (D-15). On failures open ConfirmDialog with soft-gate copy listing concrete issues (player names for unassigned; character names for duplicates; detail for mismatch). Start anyway confirms override using secondary outline styling per UI-SPEC soft-gate rule and must_haves (not destructive red; not primary accent fill); Keep recording dismisses. On success or confirm set wizardStep to nightReady without react-router navigate to /play (D-17).

    Implement NightReadyStep following PATTERNS analogs SetupStub coaching layout and ScriptHome empty/error summary cards: Display title, summary block, muted body from UI-SPEC; Back returns to record. Persist already-on session store so Phase 3 can consume. Do not add quit-before-start or dropout re-bag UI (D-16). Do not add a Play or /play CTA.
  </action>
  <acceptance_criteria>
    - NightReadyStep component exists and is reachable via wizardStep nightReady
    - RecordStep Start night path calls validateAssignments
    - Soft-gate Start anyway control uses secondary outline styling (not destructive, not primary accent fill)
    - No href or navigate call to /play from NightReadyStep or soft-gate confirm handler
    - CURSOR_DEV=true nix develop -c npm run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npm run build</automated>
  </verify>
  <done>Soft gate and Night ready UI implemented on /setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full-path E2E soft gate + Night ready + VALIDATION sync</name>
  <files>e2e/setup-record.spec.ts, e2e/setup-wizard.spec.ts, .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md (soft-gate + Night ready expected strings)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
    - e2e/setup-record.spec.ts
    - e2e/setup-wizard.spec.ts
  </read_first>
  <behavior>
    - Incomplete assignments: Start night → soft-gate heading visible → Start anyway → Night ready heading; page URL still matches /setup
    - Assign all tokens then Start night → Night ready without needing Start anyway
    - Phase-gate full suite unit+e2e green (estimated ~90–120s per VALIDATION — not a per-task sampling verify)
  </behavior>
  <action>
    Extend e2e/setup-record.spec.ts with soft-gate and Night ready cases (D-15 D-17 D-21). Assert absence of navigation to /play. Optionally assert step order smoke still holds in setup-wizard.spec.ts through deal.

    Fill 02-VALIDATION.md Per-Task Verification Map with plan/wave/task ids for SETUP-01–05 and GRIM-01–02; check off Wave 0 items that exist; leave nyquist_compliant false for validate-phase.

    Run full automated suite as the phase gate only (VALIDATION estimated runtime ~90–120s). Earlier plans already provide targeted Playwright/Vitest sampling verifies under 30s — do not treat this full-suite command as the per-task sampling cadence.
  </action>
  <acceptance_criteria>
    - e2e/setup-record.spec.ts asserts Start anyway and Night ready and /setup URL
    - CURSOR_DEV=true nix develop -c npm run test:unit exits 0
    - CURSOR_DEV=true nix develop -c npm test exits 0
    - 02-VALIDATION.md map rows no longer TBD for SETUP/GRIM requirements
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npm run test:unit && CURSOR_DEV=true nix develop -c npm test</automated>
  </verify>
  <done>Phase goal E2E proven: wizard → legal bag → record → soft gate → Night ready on /setup; validation map updated. Full suite is phase gate (~90–120s); sampling stays on earlier targeted verifies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Soft-gate confirm → nightReady | Operator override of composition mismatch |
| Persisted nightReady session → Phase 3 | Local trusted handoff blob |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering | Soft-gate bypass | high | mitigate | Require explicit Start anyway after listing issues; E2E proves dialog before nightReady on incomplete data |
| T-02-13 | Spoofing | /play navigation | medium | mitigate | Night ready never routes to /play; E2E asserts URL stays /setup |
| T-02-14 | Information Disclosure | Night ready summary | low | accept | Shows composition counts not full private bag list; ST-only device |
| T-02-15 | Elevation of Privilege | Start anyway | medium | mitigate | Override is local ST choice only; issues remain visible pre-confirm (transparency) |
</threat_model>

<verification>
CURSOR_DEV=true nix develop -c npm run test:unit
CURSOR_DEV=true nix develop -c npm test
</verification>

<success_criteria>
- Soft confirm-to-override gate (D-15) with concrete issues
- Start anyway secondary outline (not destructive / not primary accent fill)
- Night ready on /setup without /play (D-17)
- No quit-before-start feature (D-16)
- Full unit + Playwright suite green as phase gate
</success_criteria>

<output>
Create `.planning/phases/02-setup-wizard-grimoire-capture/02-05-SUMMARY.md` when done
</output>
