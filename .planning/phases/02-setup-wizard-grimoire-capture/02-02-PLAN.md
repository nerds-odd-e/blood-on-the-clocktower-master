---
phase: 02-setup-wizard-grimoire-capture
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/state/idbStorage.ts
  - src/state/setupSessionStore.ts
  - src/ui/setup/SetupWizard.tsx
  - src/ui/setup/steps/ScriptStep.tsx
  - src/ui/setup/steps/PlayersStep.tsx
  - src/ui/setup/steps/DifficultyStep.tsx
  - src/ui/setup/components/PlayerRow.tsx
  - src/ui/setup/components/ConfirmDialog.tsx
  - src/app/routes.tsx
  - src/ui/setup/SetupStub.tsx
  - e2e/setup-wizard.spec.ts
  - e2e/stubs.spec.ts
  - e2e/home.spec.ts
  - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
autonomous: true
requirements:
  - SETUP-01
  - SETUP-03
  - SETUP-05
tags:
  - setup-wizard
  - roster
  - zustand
  - playwright

must_haves:
  truths:
    - "From home Start setup, Storyteller lands on /setup script step showing Trouble Brewing confirm card, Continue setup CTA, and Back to home (D-18 D-20)"
    - "Players step uses inline list plus Add player with Up/Down seating and confirm-before-remove (D-01 D-02 D-03)"
    - "Next step is blocked when player count is outside 5–15; empty roster shows No players yet empty copy (D-04 SETUP-01 empty)"
    - "Name uniqueness is checked on Next step only using trim then JavaScript string equality; duplicates show UI-SPEC duplicate error and stay on players (SETUP-01 encoding)"
    - "Collapsed More per row exposes Experience New/Some/Veteran, Age Kid/Teen/Adult, and notes; blank/unset profiles allowed (D-04 D-08 SETUP-03)"
    - "Wizard step lives in Zustand on /setup only — no URL step segments (D-18 SETUP-05)"
    - "Persist hydration blocks interactivity until hydrated or shows dominant blank at most one frame; hydrate/parse failure shows Couldn’t restore your setup copy then fresh wizard (UI Considerations E1)"
    - "Roster scrolls vertically inside PhoneShell; sticky footer does not cover last row; long names wrap (UI Considerations E2)"
  artifacts:
    - path: "src/state/setupSessionStore.ts"
      provides: "Zustand setup session with wizardStep, players, profiles, persist to IndexedDB"
      contains: "useSetupSessionStore"
    - path: "src/ui/setup/SetupWizard.tsx"
      provides: "Multi-step wizard replacing SetupStub on /setup"
      contains: "SetupWizard"
    - path: "src/ui/setup/steps/PlayersStep.tsx"
      provides: "5–15 roster editing with seating and More profiles"
      contains: "PlayersStep"
    - path: "e2e/setup-wizard.spec.ts"
      provides: "Playwright happy-path gate through players into difficulty including SETUP-03 More profiles"
      contains: "setup wizard"
  key_links:
    - from: "src/app/routes.tsx"
      to: "src/ui/setup/SetupWizard.tsx"
      via: "/setup element={SetupWizard}"
      pattern: "SetupWizard"
    - from: "src/ui/setup/SetupWizard.tsx"
      to: "src/state/setupSessionStore.ts"
      via: "wizardStep switch"
      pattern: "wizardStep"
    - from: "src/ui/setup/SetupWizard.tsx"
      to: "src/state/setupSessionStore.ts"
      via: "hasHydrated / onFinishHydration gate; hydrate-fail → UI-SPEC persist error copy then fresh wizard"
      pattern: "hasHydrated"
    - from: "e2e/setup-wizard.spec.ts"
      to: "preview webServer"
      via: "real TB catalog path — no JSON mocks (D-21)"
      pattern: "Continue setup"
  prohibitions:
    - statement: "Must not use a separate add-player sheet or drag-and-drop seating"
      requirement_id: SETUP-01
      category: values
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-drag-drop-seating.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/players-drag-drop.bad.tsx
      check_clean_fixture: src/ui/setup/steps/PlayersStep.tsx
    - statement: "Must not feed experience/age/notes into bag generation"
      requirement_id: SETUP-03
      category: values
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-profiles-in-bag-generation.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/buildBag-with-profiles.bad.ts
      check_clean_fixture: src/state/setupSessionStore.ts
    - statement: "Must not put wizard step ids in the URL path"
      requirement_id: SETUP-05
      category: values
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-wizard-step-in-url.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/wizard-step-in-url.bad.tsx
      check_clean_fixture: src/app/routes.tsx
    - statement: "Must not render player names or notes via HTML injection APIs"
      requirement_id: SETUP-01
      category: safety
      status: resolved
      verification: judgment
    - statement: "Must not auto-assign characters to players during roster setup"
      requirement_id: SETUP-01
      category: values
      status: resolved
      verification: test
      check_kind: node-test
      check_target: tests/prohibitions/no-auto-assign-roster.test.mjs
      check_violation_fixture: tests/prohibitions/fixtures/auto-assign-roster.bad.tsx
      check_clean_fixture: src/ui/setup/steps/PlayersStep.tsx
  assumptions:
    - "SETUP-01 empty/single/null: zero players shows empty copy; Next blocked for counts 0–4 and above 15; null player entries are never stored — Add player always creates a UUID-backed row"
    - "SETUP-01 encoding: uniqueness after trim uses JS string equality (UTF-16 code units); grapheme-cluster folding is not applied"
    - "SETUP-03 empty: unset experience/age and blank notes are valid defaults until chosen"
    - "Assumption-delta: primary identity remains player name + seat order; optional profiles are add-alongside under More and do not replace seat identity"
    - "crypto.randomUUID is available in Playwright Chromium and target evergreen browsers (RESEARCH A1)"
---

## Phase Goal

**As a** Storyteller, **I want to** walk the setup wizard, get a legal TB bag, and record who drew which role before night starts, **so that** I can reach a Night ready handoff without a paper setup sheet.

<objective>
Vertical slice: replace SetupStub with SetupWizard and ship script → players (with profiles) so a Storyteller can seat a legal table and advance to difficulty (D-01–D-04, D-08, D-18, D-20).

Purpose: First user-visible wizard capability after Phase 1 stub — roster capture with persistence foundations; greens Wave 0 RED E2E from plan 02-01.
Output: Working /setup wizard through players; green Playwright for this slice; Zustand + IndexedDB session store.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-setup-wizard-grimoire-capture/02-01-SUMMARY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-PATTERNS.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md

<interfaces>
From src/domain/script/loadCatalog.ts:
- export function loadCatalog(): LoadedCatalog
- LoadedCatalog includes roles[], setupChart[] (playerCount 5–15), teamCounts

From src/app/routes.tsx (pre-change):
- Route path="/setup" currently mounts SetupStub — replace with SetupWizard

From 02-RESEARCH.md wizardStep enum:
- 'script' | 'players' | 'difficulty' | 'bag' | 'deal' | 'record' | 'nightReady'

Player row shape (create in store):
- id: string (crypto.randomUUID)
- name: string
- experience?: 'new' | 'some' | 'veteran'
- age?: 'kid' | 'teen' | 'adult'
- notes?: string (clamp length 200)

UI-SPEC primary CTAs this plan: Continue setup, Next step, Add player, Remove player / Keep player
</interfaces>
</context>

## Artifacts this phase produces

(Plan 02 — wizard shell + roster slice)

- `idbStorage` StateStorage adapter (`get`/`set`/`del` via idb-keyval)
- `useSetupSessionStore` (zustand persist, name `st-copilot-setup-session`, version 1)
- `SetupWizard`, `ScriptStep`, `PlayersStep`, `PlayerRow`, `ConfirmDialog`
- Difficulty heading shell (segments default Standard) so E2E lands — bag generation in plan 02-03
- Routes: `/setup` → `SetupWizard`; SetupStub removed/unused
- E2E green: `e2e/setup-wizard.spec.ts` through Difficulty + SETUP-03 More assertions

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Setup session store + idbStorage adapter</name>
  <files>src/state/idbStorage.ts, src/state/setupSessionStore.ts</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md (Zustand persist example, Pitfall 4 hydration)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md (D-01–D-04, D-08, D-18)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-PATTERNS.md (Zustand section)
  </read_first>
  <behavior>
    - useSetupSessionStore persists wizardStep, players (with optional experience/age/notes), difficulty default standard, bag null, assignments empty
    - Hydration gate exposes hasHydrated / onFinishHydration; parse failure resets to fresh session
  </behavior>
  <action>
    Create idbStorage.ts using idb-keyval get/set/del as Zustand StateStorage (RESEARCH example). Create useSetupSessionStore with persist name st-copilot-setup-session, version 1, createJSONStorage → idbStorage, partialize wizard fields (wizardStep, players including optional experience/age/notes per D-08 SETUP-03, difficulty default standard, bag null, assignments empty). Gate consumers on hasHydrated / onFinishHydration (Pitfall 4). On Zod/parse failure of rehydrated blob, reset to fresh session (UI surfaces error copy in Task 2).
  </action>
  <acceptance_criteria>
    - useSetupSessionStore export exists with persist middleware
    - Player type allows optional experience New/Some/Veteran, age Kid/Teen/Adult, and notes
    - CURSOR_DEV=true nix develop -c npm run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npm run build</automated>
  </verify>
  <done>Session store + IndexedDB adapter ready for wizard UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Script/players wizard UI + green roster E2E including SETUP-03</name>
  <files>src/ui/setup/SetupWizard.tsx, src/ui/setup/steps/ScriptStep.tsx, src/ui/setup/steps/PlayersStep.tsx, src/ui/setup/steps/DifficultyStep.tsx, src/ui/setup/components/PlayerRow.tsx, src/ui/setup/components/ConfirmDialog.tsx, src/app/routes.tsx, src/ui/setup/SetupStub.tsx, e2e/setup-wizard.spec.ts, e2e/stubs.spec.ts, e2e/home.spec.ts, .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md (D-01–D-04, D-08, D-18, D-20)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md (Script/Players layout, copy, ConfirmDialog, More enums, spacing, accent rules)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-PATTERNS.md (SetupWizard, PlayersStep, ConfirmDialog, PlayerRow)
    - src/ui/home/ScriptHome.tsx (CTA + empty/error card patterns)
    - src/app/routes.tsx
    - src/ui/setup/SetupStub.tsx
    - e2e/setup-wizard.spec.ts
  </read_first>
  <behavior>
    - SetupWizard waits on hasHydrated / onFinishHydration before interactive script/players/difficulty steps (blank frame at most once)
    - On rehydrate/Zod parse failure: show UI-SPEC persist-hydrate error copy containing Couldn’t restore your setup, then reset to a fresh wizard session
    - Script step: Continue setup advances wizardStep to players
    - Players: Add player appends UUID row; Up/Down reorder; Remove opens Remove {Name}? confirm; Next blocked outside 5–15; duplicates blocked on Next only
    - More expands Experience New/Some/Veteran, Age Kid/Teen/Adult, and notes; values persist in session store; blank/unset allowed (D-08 SETUP-03)
    - After five unique names, Next step shows Difficulty heading with Easy/Standard/Hard segments defaulting to Standard (D-05 D-06); bag generation is plan 02-03
  </behavior>
  <action>
    Replace SetupStub mount in routes.tsx with SetupWizard. SetupWizard switches on wizardStep inside PhoneShell (do not nest a second PhoneShell). Implement ScriptStep and PlayersStep per UI-SPEC and D-01–D-04/D-08: sticky footers, Step n of 6 label, ConfirmDialog for remove (destructive Remove player / Keep player), count gate and duplicate errors on Next. Player ids via crypto.randomUUID().

    Hydration gate (UI Considerations E1 / T-02-01): SetupWizard must read hasHydrated (or subscribe via onFinishHydration) from useSetupSessionStore and must not render interactive wizard steps until hydrated — show a dominant blank loading state for at most one frame if needed. When Task 1’s rehydrate/Zod parse path fails (or the store signals hydrate failure), SetupWizard must surface the UI-SPEC Error state (persist hydrate fail) copy whose lead phrase is Couldn’t restore your setup, then reset to a fresh wizard (script step, empty roster) so the Storyteller can continue. Happy-path E2E need not force corrupt IndexedDB; hydrate-fail UI may be covered by a focused unit/RTL test or documented judgment verify against the copy + reset wiring.

    Per D-08 SETUP-03: each PlayerRow collapsed More expands Experience (New/Some/Veteran), Age (Kid/Teen/Adult), and notes input; selections write into the session player object and survive step navigation (profiles never passed to bag builders — store fields only, D-07).

    Difficulty step: ship a real Difficulty heading + three Easy/Standard/Hard segments with default Standard and help line from UI-SPEC (D-05 D-06) so the E2E lands on a real step — bag generation remains for plan 02-03. Stop exporting or delete SetupStub once unused.

    Extend e2e/setup-wizard.spec.ts beyond the happy path: open More on at least one player; select Experience and Age enum options; fill notes; advance to Difficulty; reload or re-enter players step and assert the chosen experience/age/notes remain (SETUP-03 persistence). Match Phase 1 visual tokens (D-20). All names/notes as text nodes only.

    Update VALIDATION.md SETUP-01 and SETUP-03 map rows to plan 02 / wave 2 / this task; note SETUP-03 E2E coverage.
  </action>
  <acceptance_criteria>
    - routes.tsx mounts SetupWizard on /setup
    - SetupWizard gates interactive steps on hasHydrated / onFinishHydration (no interactive Continue setup / Add player until hydrated)
    - On rehydrate/Zod parse failure, UI shows copy containing Couldn’t restore your setup then presents a fresh wizard (script step)
    - PlayersStep enforces count gate 5–15 before advancing
    - ConfirmDialog used for remove confirmation
    - More expands Experience options New, Some, Veteran and Age options Kid, Teen, Adult plus notes field
    - e2e/setup-wizard.spec.ts asserts More expand, at least one Experience enum choice, at least one Age enum choice, notes text persistence after Next step to Difficulty (or after returning to players)
    - CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts e2e/stubs.spec.ts e2e/home.spec.ts exits 0
    - CURSOR_DEV=true nix develop -c npm run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts e2e/stubs.spec.ts e2e/home.spec.ts</automated>
  </verify>
  <done>Storyteller can confirm TB, seat 5–15 players with D-08 profiles/seating, and reach Difficulty; SETUP-03 E2E green; session persists via IndexedDB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB ↔ Zustand rehydrate | Untrusted persisted JSON crosses into session state |
| Player name/notes text ↔ React DOM | Untrusted ST-typed strings render in UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | setupSessionStore rehydrate | high | mitigate | Zod-parse persisted blob; on failure reset to empty wizard + UI-SPEC hydrate error copy |
| T-02-02 | Tampering | PlayersStep name/notes | medium | mitigate | Render as React text nodes only; clamp notes to 200 chars; no HTML injection APIs |
| T-02-03 | Information Disclosure | roster on shared device | low | accept | Single-operator ST tool; no accounts; local device assumed (ASVS L1) |
</threat_model>

<verification>
CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts e2e/stubs.spec.ts e2e/home.spec.ts
CURSOR_DEV=true nix develop -c npm run build
</verification>

<success_criteria>
- /setup is a real wizard (not SetupStub copy)
- SetupWizard waits on hasHydrated; hydrate/parse failure shows Couldn’t restore your setup then fresh wizard
- Script → players works with D-01–D-04/D-08 behaviors
- SETUP-03 More enums + notes enforced in E2E
- E2E reaches Difficulty with five unique players
- Session store + idb persist foundation in place
</success_criteria>

<output>
Create `.planning/phases/02-setup-wizard-grimoire-capture/02-02-SUMMARY.md` when done
</output>
