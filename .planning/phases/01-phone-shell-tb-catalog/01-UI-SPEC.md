---
phase: 1
slug: phone-shell-tb-catalog
status: approved
shadcn_initialized: false
preset: none
created: 2026-07-16
reviewed_at: 2026-07-16T11:12:00+07:00
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Phase goal:** Storyteller can open a phone-first offline app that knows Trouble Brewing roles, setup chart, and night-order data (PLAT-01, PLAT-02).

**Surfaces in scope:** PhoneShell chrome, home/script catalog overview, stub routes `/setup` and `/play`. Not wizard, bag, or coach UI.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (hand-rolled Tailwind primitives) |
| Icon library | none in Phase 1 (defer `lucide-react`) |
| Font | **Fraunces** (display/heading) + **Source Sans 3** (body/label) via Google Fonts self-host or `fontsource` — no Inter/Roboto/system-ui as primary |

**Rationale (locked defaults — no CONTEXT.md; discuss-phase skipped):**
- RESEARCH.md / STACK.md explicitly defer full shadcn: “Tailwind utilities first; copy one Radix/shadcn piece only when needed.” Phase 1 is a walking skeleton — no component registry tax.
- shadcn gate: `components.json` absent; greenfield Vite React app. **Decision: do not initialize shadcn this phase** (aligned with stack “What NOT to Use”). Revisit in Phase 2+ if wizard controls multiply.
- Icons deferred per RESEARCH supporting-library deferral table.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline chip padding |
| sm | 8px | Compact element spacing, label↔control gap |
| md | 16px | Default element spacing, card inner padding |
| lg | 24px | Section padding, shell horizontal inset |
| xl | 32px | Layout gaps between major blocks |
| 2xl | 48px | Major section breaks (home hero → script card) |
| 3xl | 64px | Page-level top breathing room below safe-area |

**Exceptions:**
- Touch targets: minimum **44×44px** for all tappable controls (PLAT-01 / RESEARCH Pattern 3) — may use 44 even when adjacent spacing is 8/16.
- Safe-area: padding uses `env(safe-area-inset-*)` in addition to scale tokens (not a scale token itself).
- Shell max-width: **40rem (640px)** centered on tablet; full bleed width on phone (RESEARCH PhoneShell).

---

## Typography

Exactly 4 sizes, 2 weights:

| Role | Size | Weight | Line Height | Font |
|------|------|--------|-------------|------|
| Body | 16px | 400 | 1.5 | Source Sans 3 |
| Label | 14px | 600 | 1.4 | Source Sans 3 |
| Heading | 20px | 600 | 1.2 | Fraunces |
| Display | 28px | 600 | 1.2 | Fraunces |

**Rules:**
- Product/app name on home uses **Display**.
- Script card title uses **Heading**.
- Supporting sentences, role list rows, stub copy use **Body**.
- Team badges, meta (“22 roles”, “Offline ready”) use **Label**.
- No additional weights (no 500/700). No third font family.

---

## Color

Table-lantern dark shell (matches RESEARCH PWA `theme_color` / `background_color` intent; avoids purple-glow and cream/terracotta AI defaults).

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0B0B0B` | App background, shell surface, PWA `background_color` / `theme_color` |
| Secondary (30%) | `#1A1714` | Script card, stub panels, elevated blocks |
| Accent (10%) | `#C4A35A` | Primary CTA fill/text-on-dark emphasis only (see reserved list) |
| Destructive | `#B33A3A` | Reserved for future destructive actions — **unused in Phase 1** |
| Text primary | `#F2EDE6` | Body and headings on dominant/secondary |
| Text muted | `#A39E96` | Supporting sentences, meta labels, stub “coming later” copy |
| Border subtle | `#2A2622` | Card outline only (1px); no multi-layer shadows |

**Accent reserved for (only):**
1. Primary CTA button (“Start setup”) on home
2. Focus ring on that CTA (`outline` / `ring` using accent at ≥3:1 against dominant)
3. Active/selected indicator if home shows a single selected script row (TB)

**Accent must NOT be used for:** links in stub copy, team badges, role names, borders, or decorative icons.

**Team badge colors (secondary semantic, not accent):**
| Team | Badge bg | Badge text |
|------|----------|------------|
| Townsfolk | `#1E3A2F` | `#B8D4C8` |
| Outsider | `#2A3340` | `#B0C4D8` |
| Minion | `#3A1E28` | `#E0B0B8` |
| Demon | `#3A2418` | `#E0C4A8` |

These are informational chips only; they do not replace accent for CTAs.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | **Start setup** |
| App name (Display) | **Storyteller Copilot** |
| Home supporting line | Helps you run Trouble Brewing — offline, on your phone. |
| Script card title | Trouble Brewing |
| Script card meta | 22 roles · setup chart · night order |
| Script card status | Available |
| Empty state heading | No scripts loaded |
| Empty state body | Trouble Brewing data did not load. Reinstall or reload the app after connecting once. |
| Error state | Couldn’t read script data. Reload the app. If it keeps failing, reinstall after a network connection. |
| Stub `/setup` heading | Setup |
| Stub `/setup` body | Setup wizard comes in the next phase. |
| Stub `/play` heading | Play |
| Stub `/play` body | Night coach comes after setup. |
| Destructive confirmation | none — no destructive actions in Phase 1 |

**CTA behavior:** “Start setup” navigates to `/setup` (stub). Do not label CTA “Play”, “Continue”, or “Get started”.

**Catalog content policy (Phase 1 home):**
- Show script name, availability, role **count by team** (or total 22), and that night/setup data is present.
- Optional: compact role name list grouped by team — names + team badges only.
- Do **not** dump full Almanac ability text on home. If a one-line ability is stored in JSON, omit from Phase 1 UI or show truncated single line max 80 characters with wrap (prefer omit).
- No official BotC token art.

---

## Layout & Interaction Contract

### PhoneShell
- `min-height: 100dvh`; `overflow-x: hidden`; safe-area padding on all four sides.
- Single column; horizontal padding `lg` (24px); content `max-width: 40rem`; `margin-inline: auto`.
- No bottom tab bar in Phase 1 (only three routes; home is primary). Stub pages get a text back link: **Back to home** → `/`.
- Primary CTA sits in normal document flow near the script card (thumb-reachable on phone without a sticky footer requirement this phase).

### Home composition (first viewport)
One composition, not a dashboard:
1. Display app name
2. One supporting sentence
3. One script card (Trouble Brewing) + primary CTA
4. No stats strips, schedule chips, or multi-card grids

### Routes
| Path | UI |
|------|-----|
| `/` | Home + TB catalog overview |
| `/setup` | Stub panel with copy above |
| `/play` | Stub panel with copy above |

### Motion (minimal Phase 1)
- CTA: `opacity` + `translateY(4px)` enter on first paint ≤200ms, `prefers-reduced-motion: reduce` → no motion.
- No decorative ambient animation.

### Accessibility
- Visible focus ring on interactive elements (accent on CTA; text-primary outline on Back link).
- Contrast: text primary on dominant ≥4.5:1; accent button text uses `#0B0B0B` on `#C4A35A` fill (≥4.5:1).
- Render catalog strings as text nodes only — never `dangerouslySetInnerHTML` (RESEARCH security).

---

## UI Considerations

> Populated by the ui-phase UI-consideration probe. Empty/error COPY lives in Copywriting Contract — this section covers state coverage and references those rows.

Applicable state considerations resolved: **16 covered, 4 backstop, 2 dismissed, 0 unresolved**

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| overflow | E1 | ✅ covered | PhoneShell enforces overflow-x:hidden; content scrolls vertically only |
| long-text | E1 | 🧪 backstop | Shell copy wraps within max-width 40rem — visual check at verify |
| overflow | E2 | ✅ covered | Home hero is single-column within shell; no horizontal overflow |
| long-text | E2 | ✅ covered | Display app name and supporting sentence wrap; no ellipsis clipping |
| empty | E3 | ✅ covered | If parse yields zero scripts, show Empty state heading/body from Copywriting Contract |
| loading | E3 | ✅ covered | Catalog is sync-imported at module init — no loading skeleton; first paint is populated or empty/error |
| error | E3 | ✅ covered | Failed Zod.parse shows Error state copy; Start setup CTA hidden until catalog valid |
| populated | E3 | ✅ covered | Exactly one script card: Trouble Brewing, Available, meta line, Start setup CTA |
| partial | E3 | ✅ covered | Partial/corrupt role arrays fail Zod entirely — same as error (no half-rendered card) |
| overflow | E3 | 🧪 backstop | Optional role list scrolls vertically inside shell; page never gains horizontal scroll at 390x844 |
| zero-one-many | E3 | ✅ covered | Phase 1 targets one available script with singular copy; zero scripts → empty state |
| long-text | E4 | ✅ covered | Fixed label Start setup — no dynamic truncation |
| overflow | E5 | ✅ covered | Stub /setup panel stays within PhoneShell; vertical scroll only |
| long-text | E5 | 🧪 backstop | Stub /setup body wraps — visual check at verify |
| overflow | E6 | ✅ covered | Stub /play panel stays within PhoneShell; vertical scroll only |
| long-text | E6 | 🧪 backstop | Stub /play body wraps — visual check at verify |
| loading | E7 | 🚫 dismissed | Back to home is a static React Router link — no async navigation data load |
| error | E7 | 🚫 dismissed | Back to home has no fetch/submit failure mode in Phase 1 |
| overflow | E7 | ✅ covered | Back link text wraps if needed; shell overflow-x:hidden |
| long-text | E7 | ✅ covered | Fixed label Back to home — no truncation |
| overflow | E8 | ✅ covered | Empty/error message panels stay within shell; vertical scroll only |
| long-text | E8 | ✅ covered | Empty/error copy wraps; uses Copywriting Contract strings verbatim |

### Planner lift notes
- **covered** → `must_haves.truths` string
- **backstop** → `{ statement, verification: backstop }` (held-out visual check at verify)
- **dismissed** → not lifted (reason retained here)
- **unresolved** → planner must treat as assumption


## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-07-16

---

## Decision Log (researcher defaults)

| Topic | Decision | Source |
|-------|----------|--------|
| shadcn | Do not init Phase 1 | RESEARCH/STACK “What NOT to Use”; greenfield |
| Color mode | Dark table-lantern `#0B0B0B` | RESEARCH PWA theme; phone-at-table |
| Accent | Warm candle `#C4A35A` | Discretion — avoid purple/terracotta defaults |
| Fonts | Fraunces + Source Sans 3 | Discretion — expressive, not system/Inter |
| Primary CTA | “Start setup” | Roadmap wizard path; Phase 2 owns real wizard |
| Ability text on home | Omit / do not feature | RESEARCH open question #2 recommendation |
| Token art | None | PROJECT out of scope / IP |
| Icons / Vaul / Sonner | Defer | RESEARCH Phase 1 deferral table |

*No CONTEXT.md — all visual preferences above are researcher-locked defaults for MVP Phase 1.*
