---
phase: 01-phone-shell-tb-catalog
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/index.css
  - src/App.tsx
  - src/app/App.tsx
  - src/app/routes.tsx
  - src/app/layout/PhoneShell.tsx
  - src/ui/home/ScriptHome.tsx
  - src/ui/setup/SetupStub.tsx
  - src/ui/play/PlayStub.tsx
autonomous: true
requirements:
  - PLAT-01
tags:
  - phone-shell
  - routes
  - playwright

must_haves:
  truths:
    - "Storyteller opens / on a 390x844 viewport and sees Display app name Storyteller Copilot with no horizontal page scroll"
    - "Start setup navigates to /setup stub; Back to home returns to /; /play stub is reachable by URL with Play heading"
    - "If catalog is not yet wired, home still exposes Empty and Error state panels using UI-SPEC Copywriting Contract strings"
    - statement: "Long shell copy and stub body copy wrap within max-width 40rem without horizontal scroll at 390x844"
      verification: backstop
  artifacts:
    - path: "src/app/layout/PhoneShell.tsx"
      provides: "Phone-first shell with safe-area, overflow-x hidden, max-width 40rem"
      contains: "PhoneShell"
    - path: "src/app/routes.tsx"
      provides: "Shallow /, /setup, /play route table"
      contains: "BrowserRouter"
    - path: "src/ui/home/ScriptHome.tsx"
      provides: "Home composition with brand + Empty/Error copy ready for catalog wire"
      contains: "ScriptHome"
  key_links:
    - from: "src/main.tsx"
      to: "src/app/App.tsx"
      via: "entry render after migrate off default src/App.tsx"
      pattern: "createRoot"
    - from: "src/ui/setup/SetupStub.tsx"
      to: "/"
      via: "Back to home link"
      pattern: "Back to home"
  prohibitions:
    - "Must not add VitePWA / service worker registration in this plan (plan 01-03)"
    - "Must not add Zod schemas or TB JSON catalog loaders in this plan (plan 01-03)"
    - "Must not ship Travelers or Fabled characters"
    - "Must not include official Blood on the Clocktower purple-box token art assets"
    - "Must not offer a second available script on home"
    - "Must not add Install / Add-to-Home-Screen coaching CTA (D-04)"
    - "Must not add a unit-test runner domain suite in Phase 1 (D-05 D-07)"
    - "Must not implement setup wizard, bag builder, role recording, or night coach UI"
    - "Must not show a live online/offline connectivity banner or HUD (D-03)"
    - "Must not require an account, login, or auth route"
    - "Must not dump full Almanac ability text on the home screen"
    - "Must not render catalog strings via HTML injection APIs"
  assumptions:
    - "PLAT-01 probe unclassified — treat phone-first as 390x844 primary viewport, overflow-x hidden, ≥44px tap targets, tablet same column max-width 40rem"
    - "PLAT-02 PWA + catalog Offline ready chip + green smoke land in plan 01-03; offline reload proof in 01-05"
---

## Phase Goal

**As a** new Storyteller, **I want to** open a phone-first offline app that already knows Trouble Brewing, **so that** later setup and coach phases have a real shell and catalog to build on.

<objective>
Phone-first shell vertical slice: PhoneShell, shallow stub routes, ScriptHome composition (brand + Empty/Error panels), and index.html viewport — without Zod catalog or VitePWA yet (D-09 shell; D-04 no Install CTA).

Purpose: Prove PLAT-01 shell + stub navigation before catalog/PWA green plan.
Output: Runnable preview shell with branded home composition and /setup /play stubs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-phone-shell-tb-catalog/01-01-PLAN.md
@.planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md
@.planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md
@.planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md
@.planning/phases/01-phone-shell-tb-catalog/01-PATTERNS.md
@.planning/phases/01-phone-shell-tb-catalog/01-SKELETON.md
@.planning/research/STACK.md

<interfaces>
Depends on plan 01 scaffold + RED Playwright specs.

Contracts this plan creates:

From src/app/layout/PhoneShell.tsx:
- export function PhoneShell(props: { children: React.ReactNode }): JSX.Element

Routes (react-router-dom):
- / → ScriptHome
- /setup → SetupStub
- /play → PlayStub

ScriptHome exposes Empty/Error panels and brand copy; loadCatalog wiring is plan 01-03.
</interfaces>
</context>

## Artifacts this phase produces

(Plan 02 — shell + routes + stubs)

- `PhoneShell` — `src/app/layout/PhoneShell.tsx`
- `App` / routes — `src/app/App.tsx`, `src/app/routes.tsx`
- `ScriptHome`, `SetupStub`, `PlayStub`
- `index.html` viewport-fit=cover
- Tailwind + react-router-dom + Fraunces / Source Sans 3

<tasks>

<task type="auto">
  <name>Task 1: Tailwind tokens, viewport, and PhoneShell</name>
  <files>package.json, package-lock.json, vite.config.ts, index.html, src/index.css, src/app/layout/PhoneShell.tsx</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md (colors, typography, safe-area)
    - .planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md (Tailwind v4 + fontsource)
    - index.html
    - vite.config.ts
  </read_first>
  <action>
    Install Phase 1 shell deps from RESEARCH (not a unit-test runner, not zod, not vite-plugin-pwa): tailwindcss + @tailwindcss/vite 4.3.x, react-router-dom 7.18.x, @fontsource/fraunces, @fontsource/source-sans-3. Confirm fontsource packages on npmjs (ASSUMED) before install. Do not install Zustand, Dexie, Vaul, Sonner, lucide-react, zod, or vite-plugin-pwa (those last two are plan 01-03).

    Wire Tailwind v4 via `@tailwindcss/vite` and `@import "tailwindcss"` in `src/index.css`. Define CSS variables matching UI-SPEC colors (dominant #0B0B0B, secondary #1A1714, accent #C4A35A, text primary #F2EDE6, muted #A39E96, border #2A2622) and typography roles (Display 28px Fraunces, Heading 20px Fraunces, Body 16px Source Sans 3, Label 14px Source Sans 3). Import fontsource CSS from `src/index.css` or `main.tsx` in Task 2.

    Set `index.html` viewport to width=device-width, initial-scale=1, viewport-fit=cover if not already (per D-09 / UI-SPEC). Build `PhoneShell` per UI-SPEC: min-h-dvh, overflow-x hidden, px 24px (lg), max-w 40rem centered, env(safe-area-inset-*) on all sides, dominant background. Export `PhoneShell` for Task 2 routes.
  </action>
  <acceptance_criteria>
    - PhoneShell export exists in src/app/layout/PhoneShell.tsx
    - index.html includes viewport-fit=cover (or equivalent viewport-fit token)
    - src/index.css imports tailwindcss and defines UI-SPEC CSS variables
    - `node -e "const p=require('./package.json'); const d={...(p.dependencies||{}),...(p.devDependencies||{})}; process.exit(d.vitest||d.zod||d['vite-plugin-pwa']?1:0)"` exits 0
    - `nix-shell -p nodejs --run 'npm run build'` exits 0 (may still use default App until Task 2)
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs --run 'npm run build'</automated>
  </verify>
  <done>Tailwind tokens, viewport, and PhoneShell build; zod/PWA not installed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Routes, stubs, and ScriptHome composition</name>
  <files>src/main.tsx, src/App.tsx, src/app/App.tsx, src/app/routes.tsx, src/ui/home/ScriptHome.tsx, src/ui/setup/SetupStub.tsx, src/ui/play/PlayStub.tsx, src/index.css</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md (Copywriting Contract + Routes + Empty/Error)
    - .planning/phases/01-phone-shell-tb-catalog/01-PATTERNS.md (file map)
    - src/app/layout/PhoneShell.tsx
    - src/main.tsx
    - src/App.tsx
  </read_first>
  <action>
    Implement shallow routes with BrowserRouter: `/` ScriptHome, `/setup` SetupStub, `/play` PlayStub. Move entry from default `src/App.tsx` to `src/app/App.tsx` + `routes.tsx`; update `main.tsx` accordingly (delete or thin-reexport old `src/App.tsx` so imports do not dual-mount). Wrap routes in PhoneShell. Stub copy exactly from UI-SPEC. Back to home is a text link to `/` (no tab bar).

    ScriptHome one-composition layout per UI-SPEC: Display Storyteller Copilot; supporting line Helps you run Trouble Brewing — offline, on your phone.; Empty and Error state panels using UI-SPEC Copywriting Contract strings verbatim. Do not wire loadCatalog yet — leave a clear hook for plan 01-03; Start setup may remain hidden until catalog is valid in 01-03 (smoke stays RED until then). Ensure fontsource imports are present. Render all user-visible strings as React text children only — never raw HTML injection into the DOM.
  </action>
  <acceptance_criteria>
    - routes map /, /setup, /play
    - Stub pages contain Setup / Play headings and Back to home
    - ScriptHome contains Storyteller Copilot display string plus Empty and Error UI-SPEC copy
    - `nix-shell -p nodejs ripgrep --run 'rg -l "dangerouslySetInnerHTML" src --glob "*.tsx"'` exits 1 (no matches)
    - `nix-shell -p nodejs --run 'npm run build'` exits 0
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs ripgrep --run 'npm run build && rg -l "dangerouslySetInnerHTML" src --glob "*.tsx"; test $? -eq 1'</automated>
  </verify>
  <done>Routes, stubs, and ScriptHome composition build; Empty/Error copy present; no HTML injection APIs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → node_modules | Supply-chain packages enter the client bundle |
| React tree → DOM text | User-visible strings must not use HTML injection APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | ScriptHome / stubs render | high | mitigate | Render all copy as React text children only; no HTML injection APIs |
| T-01-03 | Spoofing | Auth surface | low | accept | No accounts by design (PLAT-02); no login routes |
| T-01-SC | Tampering | npm installs (tailwind, router, fontsource) [ASSUMED] | high | mitigate | Confirm packages on npmjs before install; commit lockfile; RESEARCH Approved set |
</threat_model>

<verification>
- Build succeeds
- PhoneShell enforces overflow-x hidden and safe-area
- Routes /, /setup, /play exist with UI-SPEC stub copy
- Empty/Error states present per UI-SPEC; catalog/PWA deferred to 01-03
</verification>

<success_criteria>
- User-visible phone shell: branded home composition, stub navigation ready
- PLAT-01 skeleton: phone viewport usable without horizontal scroll
</success_criteria>

<output>
Create `.planning/phases/01-phone-shell-tb-catalog/01-02-SUMMARY.md` when done
</output>
