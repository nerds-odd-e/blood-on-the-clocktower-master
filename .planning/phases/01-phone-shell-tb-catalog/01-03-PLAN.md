---
phase: 01-phone-shell-tb-catalog
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - e2e/offline.spec.ts
  - e2e/home.spec.ts
  - vite.config.ts
  - src/main.tsx
  - .planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md
  - package.json
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
tags:
  - pwa
  - offline
  - playwright
  - validation

must_haves:
  truths:
    - "After first load of production preview, Storyteller can go offline, reload /, and still use the TB home with no account prompt (PLAT-02)"
    - "Offline reload of /setup still serves the SPA stub via navigateFallback (no blank 404)"
    - "Phone viewport 390x844 home and stubs show no horizontal document scroll (PLAT-01)"
    - "No account, login, or signup UI exists anywhere in Phase 1 routes"
    - statement: "Long display/supporting copy wraps without ellipsis clipping that hides meaning — visual check at verify"
      verification: backstop
    - statement: "Stub /setup and /play body wrap inside PhoneShell — visual check at verify"
      verification: backstop
  artifacts:
    - path: "e2e/offline.spec.ts"
      provides: "Automated offline reload proof against real SW/preview"
      contains: "setOffline"
    - path: ".planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md"
      provides: "Nyquist validation retargeted to Playwright (D-05–D-07)"
      contains: "Playwright"
  key_links:
    - from: "e2e/offline.spec.ts"
      to: "vite-plugin-pwa service worker"
      via: "First online navigation registers SW; then context offline + reload"
      pattern: "setOffline"
    - from: "workbox.navigateFallback"
      to: "/setup offline"
      via: "SPA fallback to index.html"
      pattern: "navigateFallback"
  prohibitions:
    - "Must not add Install CTA or A2HS coaching copy (D-04)"
    - "Must not gate Offline ready chip on service-worker controller state (D-02)"
    - "Must not introduce Vitest as the Phase 1 quality gate (D-05 D-07)"
    - "Must not require network after first successful load for home/stub routes"
  assumptions:
    - "PLAT-02 unclassified probe — offline acceptance is Playwright browserContext offline mode against vite preview after one online load; localhost SW is sufficient for Phase 1"
    - "PLAT-01 unclassified probe — horizontal scroll checked via Playwright evaluate of document scrollWidth vs clientWidth at 390x844"
---

<objective>
Close PLAT-02 offline proof and PLAT-01 viewport checks with Playwright (no mocks), and retarget `01-VALIDATION.md` to the Playwright-only strategy locked in CONTEXT (D-05–D-08).

Purpose: Phase gate evidence that the walking skeleton works without network after first load and remains usable on a phone viewport.
Output: Green `e2e/offline.spec.ts`; updated VALIDATION.md; full Phase 1 Playwright suite green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md
@.planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md
@.planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md
@.planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md
@.planning/phases/01-phone-shell-tb-catalog/01-SKELETON.md

<interfaces>
Depends on plans 01–02:

- Preview webServer on 4173 from playwright.config.ts
- VitePWA autoUpdate + navigateFallback already configured
- Home/catalog smoke + catalog.spec green
</interfaces>
</context>

## Artifacts this phase produces

(Plan 03 additions)

- `e2e/offline.spec.ts` — offline reload E2E
- Updated `01-VALIDATION.md` — Playwright sampling map (replaces Vitest Wave 0)
- Optional small PWA registration timing helpers in `src/main.tsx` if required for SW readiness in tests
- package.json script aliases if needed (`test:e2e`, `test:e2e:offline`)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Playwright offline reload and phone viewport suite</name>
  <files>e2e/offline.spec.ts, e2e/home.spec.ts, vite.config.ts, src/main.tsx, package.json</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md (D-01–D-04, D-08)
    - .planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md (Pitfall 1–2 offline SPA / preview-only PWA)
    - playwright.config.ts
    - vite.config.ts
    - e2e/home.spec.ts
  </read_first>
  <behavior>
    - Online first visit to / shows TB home including Offline ready chip (chip remains optimistic per D-02 — do not assert SW controller for chip)
    - After SW/controller is ready (await practical signal: controller or waitForEvent), context.setOffline(true), reload /
    - Offline / still shows Storyteller Copilot and Trouble Brewing card
    - Offline navigation or reload of /setup still shows Setup stub (navigateFallback)
    - At 390x844, document.documentElement.scrollWidth <= clientWidth on /, /setup, /play
    - No login/account UI text on those routes
  </behavior>
  <action>
    Author `e2e/offline.spec.ts` using the real preview build only (D-08). Flow: start online → load `/` once → wait until service worker is controlling the page (or equivalent readiness that is stable on vite-plugin-pwa generateSW) → `browserContext.setOffline(true)` → reload `/` and assert home still usable → open `/setup` offline and assert stub copy. Do not mock service workers or catalog.

    Extend home/smoke checks for PLAT-01: viewport 390x844; assert no horizontal overflow via scrollWidth/clientWidth on `/`, `/setup`, `/play`. Assert absence of account/login/signup affordances.

    If SW registration timing is flaky, adjust `src/main.tsx` registerSW options or Playwright waits only — do not gate the Offline ready chip on controller state (D-02). Confirm vite.config still has navigateFallback `/index.html` and autoUpdate. Prefer testing against `vite preview` after `npm run build` (RESEARCH Pitfall 2 — not `vite dev`).
  </action>
  <acceptance_criteria>
    - `e2e/offline.spec.ts` exists and calls setOffline
    - `grep -v '^#' e2e/offline.spec.ts | grep -cE 'mock|route\\.fulfill|serviceWorkers:\\s*block'` returns 0
    - `nix-shell -p nodejs --run 'npm run build && npx playwright test e2e/offline.spec.ts'` exits 0
    - `nix-shell -p nodejs --run 'npx playwright test'` exits 0 for the full e2e suite
    - Offline ready chip logic is not conditioned on `navigator.serviceWorker.controller` in ScriptHome (D-02)
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs --run 'npm run build && npx playwright test'</automated>
    <human-check>
      Optional end-of-phase visual: at 390x844 confirm long copy wraps (UI-SPEC backstops) and Offline ready chip reads as calm meta, not a second primary button (D-01).
    </human-check>
  </verify>
  <done>PLAT-02 offline reload and PLAT-01 no-horizontal-scroll proven via Playwright on real preview.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget VALIDATION.md to Playwright-only Nyquist map</name>
  <files>.planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md, package.json</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md
    - .planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md (D-05–D-07 override)
    - package.json
    - e2e/*.spec.ts
  </read_first>
  <action>
    Rewrite `01-VALIDATION.md` so Test Infrastructure, Sampling Rate, Per-Task Verification Map, and Wave 0 Requirements use Playwright E2E only — remove Vitest/jsdom/@testing-library as Phase 1 gates (D-05 D-07). Map PLAT-01 to home/stubs/viewport specs; PLAT-02 to offline.spec; catalog truths to catalog.spec. Quick run command: `nix-shell -p nodejs --run 'npx playwright test'`. Mark wave_0_complete true only if Playwright config + specs exist and full suite is green. Keep manual DevTools offline as optional supplement, not the primary gate. Ensure package.json has `test` or `test:e2e` script pointing at Playwright.
  </action>
  <acceptance_criteria>
    - `01-VALIDATION.md` frontmatter still valid YAML; framework field names Playwright
    - `grep -v '^#' .planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md | grep -c Vitest` returns 0
    - `grep -v '^#' .planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md | grep -c Playwright` ≥ 3
    - package.json scripts include an e2e/test script invoking playwright
    - `nix-shell -p nodejs --run 'npx playwright test'` exits 0
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs --run 'npx playwright test'</automated>
  </verify>
  <done>Nyquist validation contract matches CONTEXT Playwright-only decisions; full E2E suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network ↔ Service Worker cache | Offline mode must serve only previously precached trusted build assets |
| Playwright ↔ App | Tests must exercise real SW behavior without injecting malicious cached responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Denial of Service | Stale/broken SW cache | medium | mitigate | autoUpdate registration; offline E2E fails if shell missing after reload |
| T-01-11 | Tampering | Cache poisoning via mocked SW in tests | medium | mitigate | No SW mocks; real generateSW from production build (D-08) |
| T-01-12 | Spoofing | Fake offline banner encouraging install phishing | low | mitigate | No Install CTA (D-04); static Offline ready chip only (D-01–D-03) |
| T-01-05 | Elevation of Privilege | Stale SW UX | medium | mitigate | Already configured autoUpdate in plan 01; re-verified here |
| T-01-SC | Tampering | No new runtime deps expected | low | accept | Validation doc + e2e only; Playwright already installed in plan 01 |
</threat_model>

<verification>
- Full Playwright suite green including offline
- VALIDATION.md documents Playwright sampling only
- Human visual backstops noted for end-of-phase verify
</verification>

<success_criteria>
- Success criteria 1–3 from ROADMAP Phase 1 are evidenced by automated Playwright suites
- PLAT-01 and PLAT-02 requirements satisfied for the walking skeleton
</success_criteria>

<output>
Create `.planning/phases/01-phone-shell-tb-catalog/01-03-SUMMARY.md` when done
</output>
