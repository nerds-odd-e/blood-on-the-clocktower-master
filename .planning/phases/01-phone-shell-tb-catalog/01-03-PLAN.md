---
phase: 01-phone-shell-tb-catalog
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - package.json
  - vite.config.ts
  - src/main.tsx
  - src/vite-env.d.ts
  - src/ui/home/ScriptHome.tsx
  - src/domain/script/schemas.ts
  - src/domain/script/loadCatalog.ts
  - src/domain/script/index.ts
  - src/data/scripts/trouble-brewing/roles.json
  - src/data/scripts/trouble-brewing/setup-chart.json
  - src/data/scripts/trouble-brewing/procedural-beats.json
  - public/pwa-192x192.png
  - public/pwa-512x512.png
  - public/favicon.svg
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
tags:
  - tb-catalog
  - pwa
  - playwright

must_haves:
  truths:
    - "Home shows exactly one Trouble Brewing script card with status Available, meta line 22 roles · setup chart · night order, and primary CTA Start setup"
    - "Offline ready status chip appears on the TB card whenever catalog Zod parse succeeds (not gated on service-worker controller) per D-01 D-02 D-03"
    - "If catalog parse yields zero scripts, home shows Empty state heading No scripts loaded and Empty state body from UI-SPEC Copywriting Contract"
    - "Failed Zod.parse shows Error state Couldn’t read script data…; Start setup CTA hidden until catalog valid"
    - "loadCatalog exposes typed Trouble Brewing roles (22), setup chart rows for player counts 5–15, and procedural beats for later coach"
    - "Plan 01 RED smoke specs turn GREEN against real shell + catalog (D-08)"
  artifacts:
    - path: "src/domain/script/loadCatalog.ts"
      provides: "Zod-validated TB catalog load path"
      contains: "loadCatalog"
    - path: "src/ui/home/ScriptHome.tsx"
      provides: "Home composition wired to catalog + Start setup CTA + empty/error states"
      contains: "ScriptHome"
    - path: "vite.config.ts"
      provides: "VitePWA generateSW with navigateFallback and autoUpdate"
      contains: "VitePWA"
  key_links:
    - from: "src/ui/home/ScriptHome.tsx"
      to: "src/domain/script/loadCatalog.ts"
      via: "loadCatalog() at render/module init"
      pattern: "loadCatalog"
    - from: "vite.config.ts"
      to: "workbox navigateFallback"
      via: "VitePWA workbox.navigateFallback('/index.html')"
      pattern: "navigateFallback"
    - from: "e2e/home.spec.ts"
      to: "preview webServer"
      via: "Plan 01 RED specs turn GREEN against real shell — no catalog mocks (D-08)"
      pattern: "Storyteller Copilot"
  prohibitions:
    - "Must not ship Travelers or Fabled characters in the Phase 1 catalog UI or roles.json"
    - "Must not include official Blood on the Clocktower purple-box token art assets"
    - "Must not offer a second available script (BMR/SnV/custom) on home"
    - "Must not add Install / Add-to-Home-Screen coaching CTA or custom beforeinstallprompt button (D-04)"
    - "Must not add a unit-test runner domain golden suite in Phase 1 (D-05 D-07)"
    - "Must not implement setup wizard, bag builder, role recording, or night coach UI"
    - "Must not show a live online/offline connectivity banner or HUD (D-03)"
    - "Must not require an account, login, or auth route"
    - "Must not dump full Almanac ability text on the home screen"
    - "Must not render catalog strings via HTML injection APIs"
  assumptions:
    - "PLAT-02 offline reload proof is plan 01-05 — this plan wires PWA generateSW + Offline ready chip only"
    - "npm install updates package-lock.json as a side effect when adding zod / vite-plugin-pwa — commit the lockfile with package.json even though lockfile is omitted from files_modified budget"
---

## Phase Goal

**As a** new Storyteller, **I want to** open a phone-first offline app that already knows Trouble Brewing, **so that** later setup and coach phases have a real shell and catalog to build on.

<objective>
Catalog + PWA green half of the walking skeleton: Zod-validated bundled TB catalog on home (Empty/Error + Offline ready), installable PWA wiring, and green Playwright smoke from plan 01-01 (D-01–D-10, D-05–D-08).

Purpose: Prove PLAT-02 PWA wiring + TB catalog surface after shell plan 01-02.
Output: TB card home, VitePWA; smoke E2E green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-phone-shell-tb-catalog/01-02-PLAN.md
@.planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md
@.planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md
@.planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md
@.planning/phases/01-phone-shell-tb-catalog/01-PATTERNS.md
@.planning/phases/01-phone-shell-tb-catalog/01-SKELETON.md
@.planning/research/STACK.md

<interfaces>
Depends on plan 02 PhoneShell + routes + ScriptHome composition.

Contracts this plan creates:

From src/domain/script/loadCatalog.ts:
- export function loadCatalog(): TroubleBrewingCatalog
- TroubleBrewingCatalog includes scriptId "trouble-brewing", roles[], setupChart[], proceduralBeats[]

From src/domain/script/schemas.ts:
- RoleSchema fields: id, name, edition "tb", team enum townsfolk|outsider|minion|demon, ability, firstNight, otherNight, firstNightReminder, otherNightReminder, reminders[], setup
- SetupChartSchema: rows for playerCount 5–15 with townsfolk/outsiders/minions/demons counts summing to playerCount
- Catalog load fails closed (parse error → ScriptHome error state)
</interfaces>
</context>

## Artifacts this phase produces

(Plan 03 — catalog + PWA + green smoke)

- `RoleSchema`, `SetupChartSchema`, `ProceduralBeatsSchema`, `CatalogSchema`, `loadCatalog`, barrel `index.ts`
- `roles.json`, `setup-chart.json`, `procedural-beats.json`
- VitePWA config + PWA icons
- ScriptHome wired to catalog
- Plan 01 smoke specs GREEN

<tasks>

<task type="auto">
  <name>Task 1: TB catalog JSON, Zod schemas, and loadCatalog</name>
  <files>package.json, src/data/scripts/trouble-brewing/roles.json, src/data/scripts/trouble-brewing/setup-chart.json, src/data/scripts/trouble-brewing/procedural-beats.json, src/domain/script/schemas.ts, src/domain/script/loadCatalog.ts, src/domain/script/index.ts</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md (Pattern 1–3, setup chart table A1 — RESOLVED source)
    - .planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md (catalog content policy, team badges)
    - .planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md (D-10 locks)
  </read_first>
  <action>
    Install zod 4.4.x from RESEARCH (confirm on npmjs — ASSUMED). Do not install a unit-test runner. Commit updated package-lock.json alongside package.json even though lockfile is a side-effect of npm install (omitted from files_modified budget).

    Curate bundled TB data (no runtime fetch): `roles.json` with exactly 22 characters (13 townsfolk / 4 outsider / 4 minion / 1 demon), edition tb, teams limited to those four enums only; `setup-chart.json` rows for playerCount 5–15 using RESEARCH table (RESOLVED A1) — put verification source note in the `loadCatalog.ts` header comment (not inside the JSON); `procedural-beats.json` with dusk, minion-info, demon-info, dawn stubs for Phase 3. Zod schemas + `loadCatalog()` parse all three at load; export typed catalog from `src/domain/script/index.ts`.
  </action>
  <acceptance_criteria>
    - `loadCatalog` export exists
    - roles.json has exactly 22 role objects with team counts 13/4/4/1
    - setup-chart.json contains playerCount rows for 5–15
    - `node -e "const p=require('./package.json'); const d={...(p.dependencies||{}),...(p.devDependencies||{})}; process.exit(d.zod&&!d.vitest?0:1)"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs ripgrep --run 'npm run build && test "$(rg -c "\"team\": \"townsfolk\"" src/data/scripts/trouble-brewing/roles.json)" -eq 13 && test "$(rg -c "\"team\": \"outsider\"" src/data/scripts/trouble-brewing/roles.json)" -eq 4 && test "$(rg -c "\"team\": \"minion\"" src/data/scripts/trouble-brewing/roles.json)" -eq 4 && test "$(rg -c "\"team\": \"demon\"" src/data/scripts/trouble-brewing/roles.json)" -eq 1 && test "$(rg -c "\"team\":" src/data/scripts/trouble-brewing/roles.json)" -eq 22'</automated>
  </verify>
  <done>Zod-validated TB catalog load path builds with 22 roles and 13/4/4/1 teams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScriptHome catalog wire, VitePWA, and green smoke E2E</name>
  <files>src/ui/home/ScriptHome.tsx, vite.config.ts, src/main.tsx, src/vite-env.d.ts, public/pwa-192x192.png, public/pwa-512x512.png, public/favicon.svg, package.json</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md (catalog card, Offline ready, Empty/Error)
    - .planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md (D-01–D-04 offline chip; D-10)
    - src/ui/home/ScriptHome.tsx
    - src/domain/script/loadCatalog.ts
    - vite.config.ts
    - e2e/home.spec.ts
  </read_first>
  <behavior>
    - Home shows TB Available card, Offline ready chip when catalog valid, Start setup → /setup
    - loadCatalog returns 22 roles with team split 13/4/4/1
    - VitePWA has navigateFallback /index.html and registerType autoUpdate
    - Plan 01 smoke specs pass against real preview (D-08)
  </behavior>
  <action>
    Install vite-plugin-pwa 1.3.x (confirm on npmjs — ASSUMED). Wire ScriptHome to `loadCatalog`: on success show TB card (title Trouble Brewing, Available, meta 22 roles · setup chart · night order), Offline ready chip (D-01 D-02 D-03 — optimistic, not SW-gated), Start setup CTA → `/setup` with accent fill and #0B0B0B label text, min 44px height. On Zod failure show Error state and hide CTA; on zero scripts show Empty state. Optional compact role roster by team (names + team badge colors only); omit Almanac ability text per RESEARCH ability-text RESOLVED.

    Configure VitePWA in vite.config.ts: registerType autoUpdate; manifest name BotC Storyteller Copilot / short_name ST Copilot; display standalone; background_color and theme_color #0b0b0b; icons 192 and 512 (plus maskable 512); workbox navigateFallback /index.html; globPatterns including js,css,html,ico,png,svg,webp,woff2,json. Create original CCC-safe PWA PNGs and favicon (no official BotC art — D-10). Register SW from main via virtual:pwa-register. Add vite-env reference types for vite-plugin-pwa/client.

    Make plan 01 smoke specs GREEN.
  </action>
  <acceptance_criteria>
    - `nix-shell -p nodejs --run 'npm run build'` exits 0
    - Home route renders Storyteller Copilot and Offline ready when catalog valid
    - Start setup navigates to /setup; stubs match UI-SPEC body strings
    - `grep -v '^#' vite.config.ts | grep -c "navigateFallback"` ≥ 1
    - `grep -v '^#' vite.config.ts | grep -c "autoUpdate"` ≥ 1
    - `nix-shell -p nodejs --run 'npx playwright test e2e/home.spec.ts e2e/stubs.spec.ts'` exits 0
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs --run 'npm run build && npx playwright test e2e/home.spec.ts e2e/stubs.spec.ts'</automated>
  </verify>
  <done>Walking skeleton green: TB catalog, PWA config, smoke E2E green on shell from 01-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled catalog JSON → React UI | Untrusted-if-tampered static content crosses into DOM |
| Build output → Service Worker cache | Precached assets served on subsequent loads |
| npm install → node_modules | Supply-chain packages enter the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | ScriptHome catalog render | high | mitigate | Render catalog fields as React text children only; Zod.parse at load; fail to Error state on corrupt JSON |
| T-01-02 | Tampering | roles.json / setup-chart.json in repo | medium | mitigate | Zod schemas enforce shape/counts; Playwright asserts 22 roles through UI in plan 01-04 |
| T-01-04 | Information disclosure | Home ability text | medium | mitigate | Omit Almanac ability dump on home per UI-SPEC / RESEARCH RESOLVED |
| T-01-05 | Elevation of Privilege | Stale SW serving old shell | medium | mitigate | registerType autoUpdate; navigateFallback index.html for SPA |
| T-01-06 | Repudiation / Compliance | PWA icons / assets | high | mitigate | Original or CCC-safe icons only; never ship official token art |
| T-01-SC | Tampering | npm installs (vite-plugin-pwa, zod) [ASSUMED] | high | mitigate | Confirm packages on npmjs before install; commit lockfile; RESEARCH Approved set |
</threat_model>

<verification>
- Build succeeds; Playwright home+stubs green against real preview
- PWA plugin present with navigateFallback + autoUpdate
- Empty/Error states wired to catalog parse outcomes
</verification>

<success_criteria>
- User-visible walking skeleton: branded home, TB available, Start setup → stub, Back home
- PLAT-02 skeleton: PWA configured; offline proof completed in plan 01-05
</success_criteria>

<output>
Create `.planning/phases/01-phone-shell-tb-catalog/01-03-SUMMARY.md` when done
</output>
