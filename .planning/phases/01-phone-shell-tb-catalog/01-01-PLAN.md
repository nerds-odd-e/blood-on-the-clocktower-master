---
phase: 01-phone-shell-tb-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - index.html
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - eslint.config.js
  - src/main.tsx
  - src/index.css
  - src/vite-env.d.ts
  - src/app/App.tsx
  - src/app/routes.tsx
  - src/app/layout/PhoneShell.tsx
  - src/ui/home/ScriptHome.tsx
  - src/ui/setup/SetupStub.tsx
  - src/ui/play/PlayStub.tsx
  - src/data/scripts/trouble-brewing/roles.json
  - src/data/scripts/trouble-brewing/setup-chart.json
  - src/data/scripts/trouble-brewing/procedural-beats.json
  - src/domain/script/schemas.ts
  - src/domain/script/loadCatalog.ts
  - src/domain/script/index.ts
  - public/pwa-192x192.png
  - public/pwa-512x512.png
  - public/favicon.svg
  - playwright.config.ts
  - e2e/home.spec.ts
  - e2e/stubs.spec.ts
  - .planning/phases/01-phone-shell-tb-catalog/01-SKELETON.md
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
tags:
  - scaffold
  - pwa
  - phone-shell
  - tb-catalog
  - playwright

must_haves:
  truths:
    - "Storyteller opens / on a 390x844 viewport and sees Display app name Storyteller Copilot with no horizontal page scroll"
    - "Home shows exactly one Trouble Brewing script card with status Available, meta line 22 roles · setup chart · night order, and primary CTA Start setup"
    - "Start setup navigates to /setup stub; Back to home returns to /; /play stub is reachable by URL with Play heading"
    - "Offline ready status chip appears on the TB card whenever catalog Zod parse succeeds (not gated on service-worker controller) per D-01 D-02 D-03"
    - "After first online load of production build preview, Storyteller can set browser offline, reload /, and still see TB card with no account UI"
    - "loadCatalog exposes typed Trouble Brewing roles (22), setup chart rows for player counts 5–15, and procedural beats for later coach"
    - statement: "Long shell copy and optional role roster wrap within max-width 40rem without horizontal scroll at 390x844"
      verification: backstop
    - statement: "Stub /setup and /play body copy wraps within PhoneShell without horizontal overflow"
      verification: backstop
  artifacts:
    - path: "src/app/layout/PhoneShell.tsx"
      provides: "Phone-first shell with safe-area, overflow-x hidden, max-width 40rem"
      contains: "PhoneShell"
    - path: "src/domain/script/loadCatalog.ts"
      provides: "Zod-validated TB catalog load path"
      contains: "loadCatalog"
    - path: "src/ui/home/ScriptHome.tsx"
      provides: "Home composition wired to catalog + Start setup CTA"
      contains: "ScriptHome"
    - path: "vite.config.ts"
      provides: "VitePWA generateSW with navigateFallback and autoUpdate"
      contains: "VitePWA"
    - path: "e2e/home.spec.ts"
      provides: "Playwright smoke against real app (no mocks)"
      contains: "Storyteller Copilot"
  key_links:
    - from: "src/ui/home/ScriptHome.tsx"
      to: "src/domain/script/loadCatalog.ts"
      via: "loadCatalog() at render/module init"
      pattern: "loadCatalog"
    - from: "vite.config.ts"
      to: "workbox navigateFallback"
      via: "VitePWA workbox.navigateFallback('/index.html')"
      pattern: "navigateFallback"
    - from: "e2e/*.spec.ts"
      to: "preview webServer"
      via: "Playwright webServer runs real build/preview — no catalog mocks (D-08)"
      pattern: "webServer"
  prohibitions:
    - "Must not ship Travelers or Fabled characters in the Phase 1 catalog UI or roles.json"
    - "Must not include official Blood on the Clocktower purple-box token art assets"
    - "Must not offer a second available script (BMR/SnV/custom) on home"
    - "Must not add Install / Add-to-Home-Screen coaching CTA or custom beforeinstallprompt button (D-04)"
    - "Must not add Vitest domain golden unit suites in Phase 1 (D-05 D-07)"
    - "Must not implement setup wizard, bag builder, role recording, or night coach UI"
    - "Must not show a live online/offline connectivity banner or HUD (D-03)"
    - "Must not require an account, login, or auth route"
    - "Must not dump full Almanac ability text on the home screen"
    - "Must not render catalog strings via HTML injection APIs"
  assumptions:
    - "PLAT-01 probe unclassified — treat phone-first as 390x844 primary viewport, overflow-x hidden, ≥44px tap targets, tablet same column max-width 40rem"
    - "PLAT-02 probe unclassified — treat offline as Workbox precache + Playwright browserContext.setOffline(true) after first preview load; no-account means zero auth UI"
---

## Phase Goal

**As a** new Storyteller, **I want to** open a phone-first offline app that already knows Trouble Brewing, **so that** later setup and coach phases have a real shell and catalog to build on.

<objective>
Deliver the walking skeleton: Vite React phone shell, shallow stub routes, Zod-validated bundled TB catalog on home, installable PWA wiring, and a green Playwright smoke path with no mocks (D-05–D-08).

Purpose: Prove PLAT-01 + PLAT-02 end-to-end before Phase 2 wizard work.
Output: Runnable app at preview URL; `01-SKELETON.md` architectural contract; smoke E2E green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md
@.planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md
@.planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md
@.planning/phases/01-phone-shell-tb-catalog/01-PATTERNS.md
@.planning/phases/01-phone-shell-tb-catalog/01-SKELETON.md
@.planning/research/STACK.md

<interfaces>
Contracts this plan creates for later plans/phases:

From src/domain/script/loadCatalog.ts:
- export function loadCatalog(): TroubleBrewingCatalog
- TroubleBrewingCatalog includes scriptId "trouble-brewing", roles[], setupChart[], proceduralBeats[]

From src/domain/script/schemas.ts:
- RoleSchema fields: id, name, edition "tb", team enum townsfolk|outsider|minion|demon, ability, firstNight, otherNight, firstNightReminder, otherNightReminder, reminders[], setup
- SetupChartSchema: rows for playerCount 5–15 with townsfolk/outsiders/minions/demons counts summing to playerCount
- Catalog load fails closed (parse error → ScriptHome error state)

From src/app/layout/PhoneShell.tsx:
- export function PhoneShell(props: { children: React.ReactNode }): JSX.Element

Routes (react-router-dom):
- / → ScriptHome
- /setup → SetupStub
- /play → PlayStub
</interfaces>
</context>

## Artifacts this phase produces

Symbols and paths created across Phase 1 plans (this plan creates the core set):

- `PhoneShell` — `src/app/layout/PhoneShell.tsx`
- `App` — `src/app/App.tsx`
- `routes` / route elements — `src/app/routes.tsx`
- `ScriptHome` — `src/ui/home/ScriptHome.tsx`
- `SetupStub` — `src/ui/setup/SetupStub.tsx`
- `PlayStub` — `src/ui/play/PlayStub.tsx`
- `RoleSchema`, `SetupChartSchema`, `ProceduralBeatsSchema`, `CatalogSchema` — `src/domain/script/schemas.ts`
- `loadCatalog` — `src/domain/script/loadCatalog.ts`
- barrel re-exports — `src/domain/script/index.ts`
- `roles.json`, `setup-chart.json`, `procedural-beats.json` — `src/data/scripts/trouble-brewing/`
- VitePWA plugin config — `vite.config.ts`
- Playwright config — `playwright.config.ts`
- E2E specs — `e2e/home.spec.ts`, `e2e/stubs.spec.ts` (offline/catalog specs in later plans)
- PWA icons — `public/pwa-192x192.png`, `public/pwa-512x512.png`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Playwright smoke for home and stubs</name>
  <files>package.json, playwright.config.ts, e2e/home.spec.ts, e2e/stubs.spec.ts</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md (D-05–D-08 Playwright-only, no mocks)
    - .planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md (Copywriting Contract + Routes)
    - .planning/phases/01-phone-shell-tb-catalog/01-PATTERNS.md (Playwright section)
  </read_first>
  <behavior>
    - Home at / shows text Storyteller Copilot and button/link Start setup
    - Clicking Start setup lands on /setup with heading Setup and link Back to home
    - /play shows heading Play and Back to home returns to /
    - No route mocks, no catalog mocks — real webServer only (D-08)
  </behavior>
  <action>
    Confirm `@playwright/test` on npmjs.com (ASSUMED legitimacy — official Microsoft package) then install as a devDependency with the create-vite app (or after scaffold in Task 2 if package.json does not exist yet — prefer creating playwright.config + specs first against expected scripts).

    Add Playwright as Phase 1–only test layer per D-05 D-06 D-07: do not add Vitest, jsdom, or @testing-library/react.

    Create `playwright.config.ts` with: testDir `e2e`; baseURL `http://127.0.0.1:4173`; webServer command that builds then previews the Vite app on 4173 (reuseExistingServer in local). Use `nix-shell -p nodejs --run` wrappers in npm scripts if documenting shell, but package.json scripts themselves stay plain npm commands for Playwright webServer.

    Write failing `e2e/home.spec.ts` and `e2e/stubs.spec.ts` asserting UI-SPEC copy verbatim (Storyteller Copilot, Start setup, Setup / Play stubs, Back to home). Viewport 390x844 for home smoke. Commit-ready RED state expected before Task 2.
  </action>
  <acceptance_criteria>
    - `playwright.config.ts` exists and sets `testDir` to `e2e`
    - `e2e/home.spec.ts` and `e2e/stubs.spec.ts` exist and reference Storyteller Copilot, Start setup, Setup, Play, Back to home
    - `grep -v '^#' package.json | grep -c 'vitest'` returns 0
    - `grep -v '^#' package.json | grep -c '@playwright/test'` returns ≥1 after install step completes (may complete at start of Task 2 if scaffold-first)
    - Running Playwright before app scaffold fails (RED) or after empty scaffold fails assertions — not silently skipped
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs --run 'npx playwright test e2e/home.spec.ts e2e/stubs.spec.ts' ; test exit code is non-zero before Task 2 greens it</automated>
  </verify>
  <done>Failing Playwright smoke specs exist; Vitest not installed; no mocks configured.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold phone shell, TB catalog load path, and home CTA</name>
  <files>package.json, package-lock.json, vite.config.ts, index.html, src/main.tsx, src/index.css, src/vite-env.d.ts, src/app/App.tsx, src/app/routes.tsx, src/app/layout/PhoneShell.tsx, src/ui/home/ScriptHome.tsx, src/ui/setup/SetupStub.tsx, src/ui/play/PlayStub.tsx, src/data/scripts/trouble-brewing/roles.json, src/data/scripts/trouble-brewing/setup-chart.json, src/data/scripts/trouble-brewing/procedural-beats.json, src/domain/script/schemas.ts, src/domain/script/loadCatalog.ts, src/domain/script/index.ts, public/pwa-192x192.png, public/pwa-512x512.png, public/favicon.svg</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md (scaffold versions, Pattern 1–3, setup chart table, Package Legitimacy)
    - .planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md (full visual/copy/layout contract — D-09)
    - .planning/phases/01-phone-shell-tb-catalog/01-PATTERNS.md (file map + planned code patterns)
    - .planning/phases/01-phone-shell-tb-catalog/01-SKELETON.md
    - .planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md (D-01–D-04 offline chip; D-10 locks)
  </read_first>
  <action>
    Scaffold with `nix-shell -p nodejs --run "npm create vite@latest . -- --template react-ts"` in the repo root (non-interactive). Install runtime/dev versions from RESEARCH Standard Stack tables: react/react-dom 19.2.x, vite 8.1.x, @vitejs/plugin-react 6.x, typescript ~6.0.2, tailwindcss + @tailwindcss/vite 4.3.x, react-router-dom 7.18.x, zod 4.4.x, vite-plugin-pwa 1.3.x, @playwright/test (current stable). Also install @fontsource/fraunces and @fontsource/source-sans-3. Do not install Zustand, Dexie, Vaul, Sonner, lucide-react, or Vitest (D-07, deferred).

    Wire Tailwind v4 via `@tailwindcss/vite` and `@import "tailwindcss"` in `src/index.css`. Define CSS variables matching UI-SPEC colors (dominant #0B0B0B, secondary #1A1714, accent #C4A35A, text primary #F2EDE6, muted #A39E96, border #2A2622) and typography roles (Display 28px Fraunces, Heading 20px Fraunces, Body 16px Source Sans 3, Label 14px Source Sans 3).

    Set `index.html` viewport to width=device-width, initial-scale=1, viewport-fit=cover. Build `PhoneShell` per UI-SPEC: min-h-dvh, overflow-x hidden, px 24px (lg), max-w 40rem centered, env(safe-area-inset-*) on all sides, dominant background.

    Implement shallow routes with BrowserRouter: `/` ScriptHome, `/setup` SetupStub, `/play` PlayStub. Stub copy exactly from UI-SPEC. Back to home is a text link to `/` (no tab bar).

    Curate bundled TB data (no runtime fetch): `roles.json` with exactly 22 characters (13 townsfolk / 4 outsider / 4 minion / 1 demon), edition tb, teams limited to those four enums only; `setup-chart.json` rows for playerCount 5–15 using RESEARCH table — put A1 verification source note in the `loadCatalog.ts` header comment (not inside the JSON); `procedural-beats.json` with dusk, minion-info, demon-info, dawn stubs for Phase 3. Zod schemas + `loadCatalog()` parse all three at load; export typed catalog from `src/domain/script/index.ts`.

    ScriptHome one-composition layout per UI-SPEC: Display Storyteller Copilot; supporting line Helps you run Trouble Brewing — offline, on your phone.; one TB card (title Trouble Brewing, Available, meta 22 roles · setup chart · night order); Offline ready chip when parse succeeds (D-01 D-02 D-03 — optimistic, not SW-gated); Start setup CTA → `/setup` with accent fill and #0B0B0B label text, min 44px height. On Zod failure show Error state copy and hide CTA; on zero scripts show Empty state. Optional compact role roster by team (names + team badge colors only); omit Almanac ability text. Render all catalog strings as React text children only — never raw HTML injection into the DOM.

    Configure VitePWA in vite.config.ts: registerType autoUpdate; manifest name BotC Storyteller Copilot / short_name ST Copilot; display standalone; background_color and theme_color #0b0b0b; icons 192 and 512 (plus maskable 512); workbox navigateFallback /index.html; globPatterns including js,css,html,ico,png,svg,webp,woff2,json. Create original CCC-safe PWA PNGs (no official BotC art — D-10). Register SW from main via virtual:pwa-register. Add vite-env reference types for vite-plugin-pwa/client.

    Ensure package scripts: dev, build, preview, and test:e2e (playwright test). Make Task 1 smoke specs GREEN.
  </action>
  <acceptance_criteria>
    - `nix-shell -p nodejs --run 'npm run build'` exits 0
    - `loadCatalog` export exists and roles length is 22 with team counts 13/4/4/1
    - Home route renders Storyteller Copilot and Offline ready when catalog valid
    - Start setup navigates to /setup; stubs match UI-SPEC body strings
    - `rg -l 'dangerouslySetInnerHTML' src --glob '*.tsx'` exits 1 (no matches)
    - `grep -v '^#' vite.config.ts | grep -c "navigateFallback"` ≥ 1
    - `grep -v '^#' vite.config.ts | grep -c "autoUpdate"` ≥ 1
    - `nix-shell -p nodejs --run 'npx playwright test e2e/home.spec.ts e2e/stubs.spec.ts'` exits 0
    - Every roles.json team value is one of townsfolk|outsider|minion|demon (`rg -c '"team": "(townsfolk|outsider|minion|demon)"' src/data/scripts/trouble-brewing/roles.json` equals 22)
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs --run 'npm run build && npx playwright test e2e/home.spec.ts e2e/stubs.spec.ts'</automated>
  </verify>
  <done>Walking skeleton runnable: phone shell, TB catalog, stubs, PWA config, smoke E2E green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled catalog JSON → React UI | Untrusted-if-tampered static content crosses into DOM |
| Build output → Service Worker cache | Precached assets served on subsequent loads |
| npm install → node_modules | Supply-chain packages enter the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | ScriptHome catalog render | high | mitigate | Render catalog fields as React text children only; Zod.parse at load; fail to Error state on corrupt JSON |
| T-01-02 | Tampering | roles.json / setup-chart.json in repo | medium | mitigate | Zod schemas enforce shape/counts; Playwright asserts 22 roles through UI in plan 03 |
| T-01-03 | Spoofing | Auth surface | low | accept | No accounts by design (PLAT-02); no login routes to spoof |
| T-01-04 | Information disclosure | Home ability text | medium | mitigate | Omit Almanac ability dump on home per UI-SPEC; names/teams only |
| T-01-05 | Elevation of Privilege | Stale SW serving old shell | medium | mitigate | registerType autoUpdate; navigateFallback index.html for SPA |
| T-01-06 | Repudiation / Compliance | PWA icons / assets | high | mitigate | Original or CCC-safe icons only; never ship official token art |
| T-01-SC | Tampering | npm installs including @playwright/test [ASSUMED] | high | mitigate | Use RESEARCH Package Legitimacy Approved set; confirm @playwright/test and fontsource packages on npmjs before install; commit lockfile |
</threat_model>

<verification>
- Build succeeds; Playwright home+stubs green against real preview
- PhoneShell classes/styles enforce overflow-x hidden and safe-area
- PWA plugin present with navigateFallback + autoUpdate
- Catalog load path returns TB data for later phases
</verification>

<success_criteria>
- User-visible walking skeleton: branded home, TB available, Start setup → stub, Back home
- PLAT-01 skeleton: phone viewport usable without horizontal scroll
- PLAT-02 skeleton: PWA configured; offline proof completed in plan 03
</success_criteria>

<output>
Create `.planning/phases/01-phone-shell-tb-catalog/01-01-SUMMARY.md` when done
</output>
