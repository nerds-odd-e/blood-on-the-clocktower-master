---
phase: 01-phone-shell-tb-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - playwright.config.ts
  - e2e/home.spec.ts
  - e2e/stubs.spec.ts
  - .planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
tags:
  - scaffold
  - playwright
  - validation

must_haves:
  truths:
    - "Playwright config exists with e2e testDir and a preview webServer on 4173"
    - "Failing home and stubs Playwright specs assert Storyteller Copilot, Start setup, Setup, Play, and Back to home (RED until plan 01-03 greens smoke)"
    - "Phase 1 validation contract documents Playwright-only sampling — no unit-test Wave 0 gate (D-05 D-07)"
    - "create-vite react-ts scaffold exists so preview webServer can start; smoke assertions remain RED until shell/catalog plans"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright runner against real preview (no mocks)"
      contains: "testDir"
    - path: "e2e/home.spec.ts"
      provides: "RED home smoke for walking skeleton"
      contains: "Storyteller Copilot"
    - path: ".planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md"
      provides: "Nyquist map locked to Playwright (D-05–D-08)"
      contains: "Playwright"
    - path: "package.json"
      provides: "Vite react-ts scripts + @playwright/test; no unit-test runner dep"
      contains: "@playwright/test"
  key_links:
    - from: "playwright.config.ts"
      to: "vite preview webServer"
      via: "build then preview on 4173 — real app only (D-08)"
      pattern: "webServer"
    - from: "01-VALIDATION.md"
      to: "e2e/*.spec.ts"
      via: "Per-task verification map names Playwright commands"
      pattern: "Playwright"
  prohibitions:
    - "Must not add Vitest, jsdom, or @testing-library/react (D-05 D-07)"
    - "Must not mock catalog, network, or routes in Playwright config (D-08)"
    - "Must not implement PhoneShell / ScriptHome / TB catalog / VitePWA in this plan — that is plans 01-02 and 01-03"
    - "Must not require an account or auth route"
  assumptions:
    - "PLAT-01/PLAT-02 probes unclassified — this plan only establishes RED E2E + scaffold; observable phone truths land in 01-02/01-03; offline in 01-05"
    - "create-vite also emits tsconfig.json, tsconfig.app.json, tsconfig.node.json, eslint.config.js, src/vite-env.d.ts, and public/vite.svg as scaffold side effects — keep them in-repo; they are not separately planned edits and are omitted from files_modified to stay under the plan file-budget"
---

## Phase Goal

**As a** new Storyteller, **I want to** open a phone-first offline app that already knows Trouble Brewing, **so that** later setup and coach phases have a real shell and catalog to build on.

<objective>
Wave 1 RED half of the walking skeleton: Vite react-ts scaffold, Playwright smoke specs that fail until the catalog/PWA green plan (01-03), and VALIDATION.md locked to Playwright-only Nyquist sampling (D-05–D-08).

Purpose: Establish the failing E2E gate and test contract before shell/catalog/PWA work.
Output: Scaffold + RED `e2e/home.spec.ts` / `e2e/stubs.spec.ts`; Playwright-retargeted `01-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md
@.planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md
@.planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md
@.planning/phases/01-phone-shell-tb-catalog/01-PATTERNS.md
@.planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md
@.planning/phases/01-phone-shell-tb-catalog/01-SKELETON.md
@.planning/research/STACK.md

<interfaces>
Contracts this plan creates for later plans:

From playwright.config.ts:
- testDir: e2e
- baseURL: http://127.0.0.1:4173
- webServer: build + preview on 4173 (reuseExistingServer locally)

From package.json scripts:
- dev, build, preview, test:e2e (playwright test)

Scaffold only — PhoneShell / loadCatalog / VitePWA interfaces are created in plans 01-02 and 01-03.
</interfaces>
</context>

## Artifacts this phase produces

(Plan 01 — scaffold + RED)

- Vite react-ts scaffold roots (`package.json`, `vite.config.ts`, `index.html`, `src/main.tsx`, default `src/App.tsx`)
- create-vite side effects (not in files_modified budget): `tsconfig*.json`, `eslint.config.js`, `src/vite-env.d.ts`, `public/vite.svg`
- `playwright.config.ts` — real preview webServer
- `e2e/home.spec.ts`, `e2e/stubs.spec.ts` — RED until 01-03
- `01-VALIDATION.md` — Playwright-only Nyquist map (D-05–D-08)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Playwright RED smoke + VALIDATION Playwright retarget</name>
  <files>playwright.config.ts, e2e/home.spec.ts, e2e/stubs.spec.ts, .planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md, package.json</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-CONTEXT.md (D-05–D-08 Playwright-only, no mocks)
    - .planning/phases/01-phone-shell-tb-catalog/01-UI-SPEC.md (Copywriting Contract + Routes)
    - .planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md
    - .planning/phases/01-phone-shell-tb-catalog/01-PATTERNS.md (Playwright section)
  </read_first>
  <behavior>
    - Home at / shows text Storyteller Copilot and button/link Start setup
    - Clicking Start setup lands on /setup with heading Setup and link Back to home
    - /play shows heading Play and Back to home returns to /
    - No route mocks, no catalog mocks — real webServer only (D-08)
  </behavior>
  <action>
    Confirm `@playwright/test` on npmjs.com (ASSUMED legitimacy — official Microsoft package). Prefer installing it in Task 2 with the scaffold if `package.json` does not exist yet; this task may create playwright.config + specs first against expected scripts.

    Add Playwright as Phase 1–only test layer per D-05 D-06 D-07: do not add a unit-test runner, jsdom, or Testing Library packages to package.json.

    Create `playwright.config.ts` with: testDir `e2e`; baseURL `http://127.0.0.1:4173`; webServer command that builds then previews the Vite app on 4173 (reuseExistingServer in local).

    Write failing `e2e/home.spec.ts` and `e2e/stubs.spec.ts` asserting UI-SPEC copy verbatim (Storyteller Copilot, Start setup, Setup / Play stubs, Back to home). Viewport 390x844 for home smoke.

    Ensure `01-VALIDATION.md` remains Playwright-only (framework field, sampling, per-task map, Wave 0 checklist). Ban prose that names the unit-test runner is allowed; do not treat mentions of that word in ban language as a failure. Confirm framework/status assert Playwright and that no Wave 0 checklist requires a unit-test runner.
  </action>
  <acceptance_criteria>
    - `playwright.config.ts` exists and sets `testDir` to `e2e`
    - `e2e/home.spec.ts` and `e2e/stubs.spec.ts` exist and reference Storyteller Copilot, Start setup, Setup, Play, Back to home
    - `node -e "const p=require('./package.json'); const d={...(p.dependencies||{}),...(p.devDependencies||{})}; process.exit(d.vitest?1:0)"` exits 0 (no unit-test runner dependency)
    - `grep -v '^#' .planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md | grep -c Playwright` ≥ 3
    - `grep -v '^#' .planning/phases/01-phone-shell-tb-catalog/01-VALIDATION.md | grep -cE 'framework:.*[Vv]itest|Wave 0.*[Vv]itest checklist as gate'` returns 0
    - Playwright smoke is RED (non-zero exit) before plan 01-03 greens it
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs --run 'npx playwright test e2e/home.spec.ts e2e/stubs.spec.ts'; exit_code=$?; test "$exit_code" -ne 0'</automated>
  </verify>
  <done>Failing Playwright smoke specs exist; VALIDATION.md is Playwright-only; unit-test runner not installed.</done>
</task>

<task type="auto">
  <name>Task 2: create-vite react-ts scaffold without shell/catalog</name>
  <files>package.json, package-lock.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/index.css</files>
  <read_first>
    - .planning/phases/01-phone-shell-tb-catalog/01-RESEARCH.md (scaffold versions, Package Legitimacy)
    - .planning/phases/01-phone-shell-tb-catalog/01-SKELETON.md
    - .planning/research/STACK.md
  </read_first>
  <action>
    Scaffold with `nix-shell -p nodejs --run "npm create vite@latest . -- --template react-ts"` in the repo root (non-interactive), preserving existing `.planning/` and e2e files. Install create-vite baseline packages per RESEARCH (react/react-dom 19.2.x, vite 8.1.x, @vitejs/plugin-react 6.x, typescript ~6.0.2). Install `@playwright/test` as a devDependency if not already present after Task 1.

    create-vite side effects (keep in repo; not listed in this task's files budget): `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `eslint.config.js`, `src/vite-env.d.ts`, and default `public/vite.svg`. Do not delete them.

    Do not install Tailwind, react-router-dom, zod, vite-plugin-pwa, fonts, Zustand, Dexie, Vaul, Sonner, lucide-react, or a unit-test runner in this plan — Tailwind/router/fonts land in 01-02; zod/PWA in 01-03 (except Playwright already required).

    Ensure scripts: `dev`, `build`, `preview`, and `test:e2e` (playwright test). Leave default Vite App content so home/stubs smoke assertions stay RED (Storyteller Copilot UI is plan 01-02/01-03). Confirm package.json has no unit-test runner dependency key.
  </action>
  <acceptance_criteria>
    - `nix-shell -p nodejs --run 'npm run build'` exits 0
    - `grep -v '^#' package.json | grep -c '@playwright/test'` ≥ 1
    - `node -e "const p=require('./package.json'); const d={...(p.dependencies||{}),...(p.devDependencies||{})}; process.exit(d.vitest?1:0)"` exits 0
    - package.json scripts include build, preview, and test:e2e
    - `nix-shell -p nodejs --run 'npx playwright test e2e/home.spec.ts e2e/stubs.spec.ts'; exit_code=$?; test "$exit_code" -ne 0'` still holds (RED)
  </acceptance_criteria>
  <verify>
    <automated>nix-shell -p nodejs --run 'npm run build && (npx playwright test e2e/home.spec.ts e2e/stubs.spec.ts; exit_code=$?; test "$exit_code" -ne 0)'</automated>
  </verify>
  <done>Vite scaffold builds; Playwright remains RED; no unit-test runner; ready for shell plan 01-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → node_modules | Supply-chain packages enter the client toolchain |
| Playwright → preview server | Test harness must not inject mocked app responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs including @playwright/test [ASSUMED] | high | mitigate | Confirm @playwright/test on npmjs before install; use RESEARCH Approved create-vite set; commit lockfile |
| T-01-03 | Spoofing | Auth surface | low | accept | No accounts by design (PLAT-02); scaffold has no login routes |
| T-01-07 | Tampering | E2E harness | medium | mitigate | No route/catalog mocks in playwright.config (D-08); real webServer only |
</threat_model>

<verification>
- Scaffold builds; Playwright home+stubs exit non-zero (RED)
- VALIDATION.md documents Playwright sampling; package.json has no unit-test runner dependency
</verification>

<success_criteria>
- RED E2E gate and Playwright Nyquist contract exist before green shell/catalog work
- PLAT-01/PLAT-02 scaffolding path started (full truths in later plans)
</success_criteria>

<output>
Create `.planning/phases/01-phone-shell-tb-catalog/01-01-SUMMARY.md` when done
</output>
