---
phase: 03-night-coach-live-grimoire
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/data/scripts/trouble-brewing/coach-copy.json
  - src/domain/script/schemas.ts
  - src/domain/script/loadCatalog.ts
  - src/domain/coach/composePrompt.ts
  - src/domain/coach/composePrompt.test.ts
  - src/domain/coach/index.ts
  - src/ui/play/CoachBeatView.tsx
  - src/ui/play/PlayScreen.tsx
  - src/state/setupSessionStore.ts
autonomous: true
requirements:
  - COACH-02
  - COACH-03
tags:
  - night-coach
  - prompts
  - expand

must_haves:
  truths:
    - "Next-beat screen shows current step title, short prompt, and primary sticky Next (COACH-02, D-01, D-03)"
    - "Storyteller can tap More detail / Less detail to inline-expand procedure detail without a Vaul sheet (COACH-03, D-02)"
    - "Header shows night meta First night · step {n} of {total} or Other night · step {n} of {total} plus secondary Grimoire text link that is not accent-weighted (D-04)"
    - "Empty coach (no beats / no assignments) shows Nothing to coach yet heading and record-at-least-one-role body"
    - "Partial grimoire: only recorded wakes appear; procedural beats still gate correctly"
    - "0 beats → empty coach; 1+ beats → meta step n of total with a single current beat card"
    - "Long detail and content scroll above sticky footer; overflow-x hidden on play shell"
    - "Persist write fail mid-play shows soft banner Couldn’t save just now… while coach remains usable"
    - "Expanded detail wraps as Body; catalog reminders are not truncated mid-word"
    - "Header meta + Grimoire link wrap within shell width"
    - "Optional Back decrements beatIndex with clamp (D-08 — ship index-based Back)"
    - "Next at end of queue sets playSurface to bridge (bridge UI may be minimal placeholder until 03-05)"
    - "Coach copy is paraphrased ST coaching from coach-copy.json + catalog reminders — not Almanac dumps"
    - statement: "Long role and player names wrap in Heading/sub-label at 390×844 without horizontal scroll"
      verification: backstop
  artifacts:
    - path: "src/data/scripts/trouble-brewing/coach-copy.json"
      provides: "IP-safe short/detail templates"
      contains: "dusk"
    - path: "src/domain/coach/composePrompt.ts"
      provides: "PromptComposer short+detail"
      exports: ["composePrompt"]
    - path: "src/ui/play/CoachBeatView.tsx"
      provides: "Full-screen beat card + sticky Next"
      contains: "More detail"
  key_links:
    - from: "CoachBeatView"
      to: "composePrompt"
      via: "short/detail from beat + nightKind"
      pattern: "composePrompt"
    - from: "CoachBeatView Next"
      to: "advanceBeat / beatIndex"
      via: "store mutation; end → playSurface bridge"
      pattern: "beatIndex"
    - from: "PlayScreen"
      to: "CoachBeatView"
      via: "playSurface === coach"
      pattern: "CoachBeatView"
  prohibitions:
    - "Vaul or bottom-sheet library for expand"
    - "Permanent player strip or dashboard split landing"
    - "Accent color on Grimoire header link or closed Expand label"
    - "Day-phase nomination/vote chrome"
    - "Official BotC token art"
    - "ASSIST auto-answers in detail copy"
  assumptions:
    - "SPEC probe COACH-02/COACH-03 → truths above from UI-SPEC E2/E3/E9"
    - "Bridge full Night complete UI lands in 03-05; this plan may set playSurface bridge with a minimal stub label if needed"
---

## Phase Goal

**As a** Storyteller, **I want to** run First Night and Other Nights via a next-beat coach with a live grimoire, **so that** I always know what to do next and what to say without a paper night sheet.

<objective>
Vertical slice: ship full-screen next-beat coach with short prompt, inline expand detail, sticky Next, and Back (D-01–D-04, D-08, COACH-02, COACH-03).

Purpose: Answer “what do I do next / what do I say?” on every beat.
Output: coach-copy.json + composePrompt; CoachBeatView; PlayScreen coach chrome.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-night-coach-live-grimoire/03-UI-SPEC.md
@.planning/phases/03-night-coach-live-grimoire/03-CONTEXT.md
@.planning/phases/03-night-coach-live-grimoire/03-RESEARCH.md
@.planning/phases/03-night-coach-live-grimoire/03-PATTERNS.md
@src/ui/play/PlayScreen.tsx
@src/ui/setup/steps/RecordStep.tsx
@src/domain/script/schemas.ts
@src/data/scripts/trouble-brewing/procedural-beats.json

<interfaces>
composePrompt(beat, ctx) → { short: string, detail: string }
UI-SPEC expand labels: More detail / Less detail with aria-expanded
UI-SPEC primary CTA: Next (accent sticky min-h-12)
UI-SPEC secondary: Back (underline, not accent); Grimoire (header text link)
Night meta templates exactly as UI-SPEC Copywriting Coach chrome
Motion: opacity + translateY(4px) ≤200ms; respects prefers-reduced-motion
Render names/prompts as React text nodes only — never inject HTML strings
</interfaces>
</context>

## Artifacts this phase produces

(Plan 03)

- `coach-copy.json` entries for dusk, minion-info, demon-info, dawn, and wake templates
- Zod schema + load path for coach copy (via loadCatalog or sibling loader)
- `composePrompt` — `src/domain/coach/composePrompt.ts`
- `CoachBeatView` — beat card + expand + sticky footer
- Store helpers: `advanceBeat`, `retreatBeat` (or equivalent), clamp beatIndex when queue shrinks
- PlayScreen coach header/footer integration

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: coach-copy data + composePrompt green</name>
  <files>src/data/scripts/trouble-brewing/coach-copy.json, src/domain/script/schemas.ts, src/domain/script/loadCatalog.ts, src/domain/coach/composePrompt.ts, src/domain/coach/composePrompt.test.ts, src/domain/coach/index.ts</files>
  <read_first>
    - .planning/phases/03-night-coach-live-grimoire/03-UI-SPEC.md (Short / detail prompt policy table — locked paraphrase templates)
    - .planning/phases/03-night-coach-live-grimoire/03-RESEARCH.md (PromptComposer, IP pitfall)
    - .planning/phases/03-night-coach-live-grimoire/03-PATTERNS.md (coach-copy + schema load)
    - src/domain/coach/composePrompt.test.ts
    - src/domain/script/schemas.ts
    - src/domain/script/loadCatalog.ts
  </read_first>
  <behavior>
    - composePrompt returns short and detail for procedure dusk matching UI-SPEC paraphrase intent
    - Wake beats substitute Role/Name and include ST-judges language in detail (no auto Empath answer)
    - Spy wake detail warns to orient privacy; no shutter view
    - Missing copy falls back to catalog firstNightReminder/otherNightReminder or procedural notes without throwing for known TB roles
  </behavior>
  <action>
    Author paraphrased coach-copy.json covering procedural ids and wake templates per UI-SPEC policy (Claude discretion locked in UI-SPEC). Add Zod schema and parse at load time like procedural-beats (fail-fast).

    Implement composePrompt pure function; green composePrompt.test.ts from 03-01. Keep copy IP-safe — paraphrase only; do not paste scraped Almanac essays. Export from domain/coach/index.ts.
  </action>
  <acceptance_criteria>
    - coach-copy.json exists under trouble-brewing and includes dusk
    - composePrompt exported and CURSOR_DEV=true nix develop -c npm run test:unit -- src/domain/coach/composePrompt.test.ts exits 0
    - Detail strings for info wakes include the phrase ST judges (case-insensitive match acceptable)
    - No new npm dependencies added to package.json
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npm run test:unit -- src/domain/coach/composePrompt.test.ts</automated>
  </verify>
  <done>Prompt composer greens with IP-safe copy data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CoachBeatView full-screen next-beat UX</name>
  <files>src/ui/play/CoachBeatView.tsx, src/ui/play/PlayScreen.tsx, src/state/setupSessionStore.ts</files>
  <read_first>
    - .planning/phases/03-night-coach-live-grimoire/03-UI-SPEC.md (Layout /play shell, Beat card, Motion, Accessibility, Copywriting Coach chrome)
    - .planning/phases/03-night-coach-live-grimoire/03-PATTERNS.md (RecordStep sticky footer + PlayerRow expand)
    - src/ui/play/PlayScreen.tsx
    - src/ui/setup/steps/RecordStep.tsx
    - src/ui/setup/components/PlayerRow.tsx
    - src/state/setupSessionStore.ts
  </read_first>
  <behavior>
    - Coach landing: one beat card (title, optional player sub-label, short, More detail toggle, sticky Next)
    - Expand toggles Less detail and reveals detail inline in the same card (D-02)
    - Next advances beatIndex; at end sets playSurface bridge
    - Back decrements beatIndex with floor 0 when beatIndex &gt; 0
    - Grimoire header link sets playSurface grimoire (panel may be placeholder until 03-05)
    - mid-play persistWriteStatus error shows UI-SPEC soft banner above beat
  </behavior>
  <action>
    Implement CoachBeatView per UI-SPEC (D-01–D-04, D-08, D-19). Wire PlayScreen to render CoachBeatView when playSurface is coach. Use composePrompt for short/detail. Sticky footer pattern from RecordStep (accent Next only).

    Add store advance/retreat (or inline setters) and clamp beatIndex when re-derived queue length shrinks (Pitfall 4). Ship Back as index decrement (UI-SPEC lock). Do not add Vaul. Do not implement Demon bluff picker chips here (03-04) — Demon Info may show title/short/detail only until then.

    Prefer data-testid hooks needed by e2e/play-coach.spec.ts if the RED spec already names them; otherwise use accessible roles/names from UI-SPEC copy.
  </action>
  <acceptance_criteria>
    - CoachBeatView.tsx contains More detail and Next
    - PlayScreen imports CoachBeatView
    - CURSOR_DEV=true nix develop -c npm run test:unit exits 0
    - grep -v '^#' CoachBeatView.tsx | grep -c 'vaul' returns 0 (case-insensitive: no vaul import)
    - Primary Next button uses bg-[var(--color-accent)] or equivalent accent token class
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npm run test:unit</automated>
  </verify>
  <done>Storyteller can read short prompts, expand detail, and advance Next/Back on /play.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog/coach-copy → UI text | Static bundled content rendered as text |
| Player names → coach sub-label | Untrusted local strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering / XSS | CoachBeatView text render | medium | mitigate | Render player names and prompts as React text children only; ban HTML injection APIs in play UI |
| T-03-05 | Information Disclosure | Spy wake | low | accept | Detail warns to orient privacy; dedicated shutter deferred per UI-SPEC |
| T-03-SC | Tampering | npm installs | high | accept | No new packages; do not add Vaul |
</threat_model>

<verification>
composePrompt unit green; CoachBeatView shows short + expand + sticky Next; no Vaul.
</verification>

<success_criteria>
On /play the Storyteller always sees the current step, a short prompt, expandable detail, and Next.
</success_criteria>

<source_audit>
SOURCE    | ID      | Item                    | Plan | Status
--------- | ------- | ----------------------- | ---- | ------
REQ       | COACH-02| Next-beat UI            | 03   | COVERED
REQ       | COACH-03| Expand detail           | 03   | COVERED
CONTEXT   | D-01..04/D-08 | Layout + Back     | 03   | COVERED
UI        | E2/E3/E9| Coach + expand + chrome | 03   | COVERED
</source_audit>

<output>
Create `.planning/phases/03-night-coach-live-grimoire/03-03-SUMMARY.md` when done
</output>
