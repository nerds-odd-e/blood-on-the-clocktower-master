---
phase: 03-night-coach-live-grimoire
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/domain/engine/types.ts
  - src/domain/engine/buildNightBeats.ts
  - src/domain/engine/buildNightBeats.test.ts
  - src/domain/engine/index.ts
  - src/state/setupSessionStore.ts
  - src/state/setupSessionSemantics.ts
  - src/state/setupSessionSemantics.test.ts
  - src/ui/setup/steps/NightReadyStep.tsx
  - src/ui/play/PlayScreen.tsx
  - src/ui/play/PlayStub.tsx
  - src/app/routes.tsx
autonomous: true
requirements:
  - COACH-01
  - COACH-04
tags:
  - night-coach
  - beat-queue
  - handoff
  - zustand

must_haves:
  truths:
    - "App derives First Night and Other Nights wake order from recorded in-play roles plus gated procedural beats (COACH-01, D-07, D-09)"
    - "Drunk wakes on believed Townsfolk role; no drunk wake step is emitted"
    - "Minion Info and Demon Info appear only on first night when playerCount >= 7 and the relevant team is recorded in play"
    - "5-player first night omits Minion Info and Demon Info even if Imp/Minion tokens exist"
    - "Other Nights omit dead players; Ravenkeeper appears only when that player is in diedTonightIds"
    - "Night ready persist saving state shows saving body copy and disables Start first night while persistWriteStatus is saving"
    - "Night ready persist error shows error body plus Retry; Start first night remains available when in-memory session is valid"
    - "Night ready summary and sticky Start first night stay in PhoneShell with vertical scroll only — no horizontal overflow"
    - "Night ready summary values wrap as Body with no horizontal scroll"
    - "Start first night sets nightKind first, beatIndex 0, playSurface coach, playStarted true and navigates to /play (D-05, D-17)"
    - "Partial grimoire: wakes only from recorded assignments — never invents players (D-18)"
    - "Play landing shows the current derived beat title from buildNightBeats (not a frozen Beat array)"
  artifacts:
    - path: "src/domain/engine/buildNightBeats.ts"
      provides: "Pure night beat queue builder"
      exports: ["buildNightBeats"]
    - path: "src/domain/engine/types.ts"
      provides: "NightKind and Beat discriminated union"
      contains: "NightKind"
    - path: "src/ui/play/PlayScreen.tsx"
      provides: "Replaces PlayStub on /play"
      contains: "PlayScreen"
    - path: "src/ui/setup/steps/NightReadyStep.tsx"
      provides: "Start first night sticky CTA"
      contains: "Start first night"
  key_links:
    - from: "NightReadyStep.startFirstNight"
      to: "navigate('/play')"
      via: "store action then React Router navigate"
      pattern: "startFirstNight"
    - from: "PlayScreen"
      to: "buildNightBeats"
      via: "re-derive queue each render from session + catalog"
      pattern: "buildNightBeats"
    - from: "setupSessionStore persist"
      to: "version 2 migrate"
      via: "Zustand persist version/migrate for play fields"
      pattern: "version: 2"
  prohibitions:
    - "Day-phase nomination, vote, or execution UI"
    - "Frozen Beat array persisted as source of truth"
    - "Always showing full night sheet empty not-in-play slots"
    - "New npm package installs this phase"
    - "Second Zustand store for play state"
  assumptions:
    - "Planner lock A1: Ravenkeeper other-night inclusion requires diedTonightIds membership"
    - "Planner lock A2: Scarlet Woman stays in Other Night queue whenever alive; coach-copy conditional detail accepted in 03-03"
    - "Planner lock A3: Star-pass is coach copy guidance only — no auto role-swap engine in this phase"
    - "SPEC probe COACH-01 unresolved → covered by engine truths above"
---

## Phase Goal

**As a** Storyteller, **I want to** run First Night and Other Nights via a next-beat coach with a live grimoire, **so that** I always know what to do next and what to say without a paper night sheet.

## Plan notes

**Intentional density (scope_sanity):** `files_modified` is 11 (above the soft threshold of 10). Keep as one plan — this is the MVP vertical slice for Start first night → derived beat on /play. Splitting engine vs handoff UI would leave the CTA landing on a stub without `buildNightBeats`, breaking the shippable user path. Engine goldens (Task 1) and persist/CTA/PlayScreen (Task 2) stay sequential in the same plan.

<objective>
Vertical slice: green COACH-01 beat derivation (and COACH-04 Minion/Demon Info gating in the engine) and let the Storyteller tap Start first night to land on /play showing the current derived beat (D-05, D-07, D-09, D-17, D-18).

Purpose: Deliver the rules-correct queue + handoff before coach chrome polish.
Output: domain/engine module; session persist v2 play fields; Night ready CTA; PlayScreen replacing PlayStub.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-night-coach-live-grimoire/03-CONTEXT.md
@.planning/phases/03-night-coach-live-grimoire/03-RESEARCH.md
@.planning/phases/03-night-coach-live-grimoire/03-UI-SPEC.md
@.planning/phases/03-night-coach-live-grimoire/03-PATTERNS.md
@.planning/phases/03-night-coach-live-grimoire/03-01-PLAN.md
@src/state/setupSessionStore.ts
@src/ui/setup/steps/NightReadyStep.tsx
@src/ui/play/PlayStub.tsx
@src/app/routes.tsx
@src/domain/bag/buildBag.ts

<interfaces>
buildNightBeats(nightKind, input, catalog) → Beat[]
Beat = procedure { id: dusk|minion-info|demon-info|dawn } | wake { id, roleId, playerId, label }
wakeRoleId(assignment) = believedRoleId ?? trueRoleId ?? bagRoleId
Store play fields: nightKind, beatIndex, playSurface ('coach'|'grimoire'|'bridge'), deadPlayerIds, reminders, demonBluffs, diedTonightIds, playStarted
startFirstNight(): sets first/0/coach/true
UI-SPEC Night ready saved body: Assignments are saved. When the table is ready, start the first night.
Primary CTA label exactly: Start first night
</interfaces>
</context>

## Artifacts this phase produces

(Plan 02)

- `NightKind`, `Beat`, `BuildNightBeatsInput` — `src/domain/engine/types.ts`
- `buildNightBeats`, `wakeRoleId` (helper) — `src/domain/engine/buildNightBeats.ts`
- barrel `src/domain/engine/index.ts`
- Store actions: `startFirstNight`, play field defaults + persist `version: 2` + `migrate`
- `PlayScreen` component; routes `/play` → PlayScreen; remove or stop exporting PlayStub
- Night ready sticky CTA wired to startFirstNight + navigate

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement buildNightBeats and green unit goldens</name>
  <files>src/domain/engine/types.ts, src/domain/engine/buildNightBeats.ts, src/domain/engine/buildNightBeats.test.ts, src/domain/engine/index.ts</files>
  <read_first>
    - .planning/phases/03-night-coach-live-grimoire/03-RESEARCH.md (Code Examples beat queue, Pattern 2 gating, Pattern 3 Drunk, Pitfalls 1–4, Assumptions A1–A3)
    - .planning/phases/03-night-coach-live-grimoire/03-PATTERNS.md (engine analogs)
    - src/domain/engine/buildNightBeats.test.ts (from 03-01)
    - src/domain/grimoire/types.ts
    - src/domain/script/loadCatalog.ts
    - src/data/scripts/trouble-brewing/procedural-beats.json
  </read_first>
  <behavior>
    - Queue always bookends with dusk then role wakes then dawn
    - First night + count &gt;= 7 + hasMinion → minion-info; + hasDemon → demon-info; omit when count &lt; 7 (D-09)
    - Wakes sorted by firstNight/otherNight ordinals; filter ordinal &lt;= 0
    - Drunk uses believedRoleId for wake role; grimoire truth unchanged
    - Other night: skip deadPlayerIds except ravenkeeper when in diedTonightIds (A1)
    - Partial assignments: only recorded players contribute wakes (D-18)
  </behavior>
  <action>
    Implement pure `buildNightBeats` per RESEARCH shape and CONTEXT D-07/D-09/D-18. Export types from types.ts and re-export from index.ts matching bag barrel style.

    Resolve hasMinion/hasDemon from recorded assignments using trueRoleId ?? bagRoleId team lookup (not wake believed role alone for demon/minion presence). Do not persist Beat arrays. Do not invent players for unassigned seats.

    Green all cases in buildNightBeats.test.ts from plan 03-01 (adjust test imports only if path differs). Keep module free of Zustand imports.
  </action>
  <acceptance_criteria>
    - src/domain/engine/buildNightBeats.ts exports buildNightBeats
    - CURSOR_DEV=true nix develop -c npm run test:unit -- src/domain/engine/buildNightBeats.test.ts exits 0
    - A 5-player first-night fixture with Imp does not include procedure id minion-info or demon-info
    - A 7-player first-night fixture with Imp + minion includes both info beats
    - Drunk→Washerwoman fixture emits wake roleId washerwoman not drunk
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npm run test:unit -- src/domain/engine/buildNightBeats.test.ts</automated>
  </verify>
  <done>COACH-01 engine greens against real catalog goldens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist v2 play fields + Start first night → PlayScreen beat landing</name>
  <files>src/state/setupSessionStore.ts, src/state/setupSessionSemantics.ts, src/state/setupSessionSemantics.test.ts, src/ui/setup/steps/NightReadyStep.tsx, src/ui/play/PlayScreen.tsx, src/ui/play/PlayStub.tsx, src/app/routes.tsx</files>
  <read_first>
    - .planning/phases/03-night-coach-live-grimoire/03-UI-SPEC.md (Night ready handoff; /play shell; Partial grimoire safety)
    - .planning/phases/03-night-coach-live-grimoire/03-PATTERNS.md (store v2 migrate excerpt)
    - src/state/setupSessionStore.ts
    - src/state/setupSessionSemantics.ts
    - src/ui/setup/steps/NightReadyStep.tsx
    - src/ui/setup/SetupWizard.tsx
    - src/ui/setup/steps/RecordStep.tsx (sticky footer pattern)
    - src/app/routes.tsx
  </read_first>
  <behavior>
    - Persist version 2 migrate fills nightKind first, beatIndex 0, playSurface coach, empty dead/reminders/bluffs/diedTonight, playStarted false
    - startFirstNight sets first night cursor and playStarted true
    - Night ready shows Start first night sticky accent CTA; saved body copy per UI-SPEC; CTA disabled while saving
    - /play renders PlayScreen (not PlayStub); after start, shows current beat label from buildNightBeats at beatIndex
    - Empty assignments → Empty coach heading Nothing to coach yet with UI-SPEC body
  </behavior>
  <action>
    Extend PersistedSetupSessionSchema, freshSession, partializedSession, and Zustand persist to version 2 with migrate per RESEARCH (D discretion — single store). Add startFirstNight action. Extend assertSetupSessionSemantics only as needed for new array fields shape (bluff eligibility hard checks can wait for 03-04).

    Update NightReadyStep: replace “coaching arrives later” saved copy with UI-SPEC saved body; add sticky footer primary Start first night (D-05, D-19) that calls startFirstNight then navigate('/play'); disable while persistWriteStatus === saving; keep Back to record and Retry on error (E1).

    Create PlayScreen replacing PlayStub in routes.tsx. Thin landing (vertical slice): hydrate gate like SetupWizard; re-derive beats via buildNightBeats; show night meta stub + current beat Heading label (or empty coach copy). playSurface coach only for now — grimoire/bridge land in later plans. Delete or stop routing PlayStub.

    Do not implement full expand/Next/bluff picker yet (03-03/03-04). Optional minimal Next that increments beatIndex is allowed if needed to keep PlayScreen usable, but prefer leaving full coach chrome to 03-03 unless E2E from 03-01 already requires Next for green — if so, implement only index advance + clamp, not bluff UI.
  </action>
  <acceptance_criteria>
    - setupSessionStore persist config uses version 2
    - NightReadyStep contains Start first night
    - routes.tsx renders PlayScreen for /play
    - PlayScreen imports or calls buildNightBeats
    - CURSOR_DEV=true nix develop -c npm run test:unit exits 0 for setupSessionSemantics and engine tests
    - After startFirstNight path, PlayScreen renders a beat title derived from buildNightBeats
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npm run test:unit && rg -q 'Start first night' src/ui/setup/steps/NightReadyStep.tsx && rg -q 'buildNightBeats' src/ui/play/PlayScreen.tsx && rg -q 'PlayScreen' src/app/routes.tsx</automated>
  </verify>
  <done>Storyteller can Start first night and land on a derived beat on /play.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB → session merge | Untrusted persisted JSON crosses into app state |
| UI → store mutations | ST device-local only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | setupSessionStore merge | medium | mitigate | Zod PersistedSetupSessionSchema v2 + assertSetupSessionSemantics; hydrationError → fresh session |
| T-03-03 | Spoofing | Night bridge absence | low | mitigate | PlayScreen must not render nomination/vote chrome; coach/bridge only |
| T-03-SC | Tampering | npm installs | high | accept | No new packages |
</threat_model>

<verification>
Unit goldens green for buildNightBeats; Night ready CTA and PlayScreen wired; persist version 2 present.
</verification>

<success_criteria>
Storyteller can leave Night ready and see a rules-derived first-night beat on /play without a paper sheet.
</success_criteria>

<source_audit>
SOURCE    | ID      | Item                         | Plan | Status
--------- | ------- | ---------------------------- | ---- | ------
REQ       | COACH-01| Derive wake order            | 02   | COVERED
REQ       | COACH-04| Minion/Demon Info gating     | 02   | COVERED
CONTEXT   | D-05/07/09/17/18 | Handoff + filter + gate | 02 | COVERED
RESEARCH  | Engine  | buildNightBeats + persist v2 | 02   | COVERED
UI        | E1      | Night ready loading/error/overflow | 02 | COVERED
</source_audit>

<output>
Create `.planning/phases/03-night-coach-live-grimoire/03-02-SUMMARY.md` when done
</output>
