---
phase: 02-setup-wizard-grimoire-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - e2e/setup-wizard.spec.ts
  - e2e/stubs.spec.ts
  - e2e/home.spec.ts
  - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
autonomous: true
requirements:
  - SETUP-01
  - SETUP-05
tags:
  - setup-wizard
  - vitest
  - playwright
  - wave-0

must_haves:
  truths:
    - "Wave 0 deps zustand, idb-keyval, and vitest are installed with test:unit script and vitest.config.ts node environment"
    - "e2e/setup-wizard.spec.ts exists as RED gate: Start setup → Continue setup → five players → Difficulty heading (D-21)"
    - "e2e/stubs.spec.ts and e2e/home.spec.ts expect Trouble Brewing confirm + Continue setup + Back to home on /setup (not Setup stub-only heading)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Node Vitest runner for domain tests"
      contains: "environment: 'node'"
    - path: "e2e/setup-wizard.spec.ts"
      provides: "Playwright RED happy-path scaffold through players into difficulty"
      contains: "Continue setup"
    - path: "package.json"
      provides: "zustand, idb-keyval, vitest, test:unit"
      contains: "test:unit"
  key_links:
    - from: "e2e/setup-wizard.spec.ts"
      to: "preview webServer"
      via: "real TB catalog path — no JSON mocks (D-21)"
      pattern: "Continue setup"
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test:unit → vitest run"
      pattern: "test:unit"
  prohibitions:
    - statement: "Must not implement SetupWizard UI or session store in this plan — RED E2E and deps only"
      requirement_id: SETUP-05
      category: values
      status: resolved
      verification: judgment
    - statement: "Must not import catalog JSON into Playwright specs"
      requirement_id: SETUP-05
      category: safety
      status: resolved
      verification: judgment
  assumptions:
    - "idb-keyval and vitest [SUS] age flags are approved per RESEARCH Package Legitimacy Audit — no human checkpoint"
    - "Wizard UI greening happens in plan 02-02"
---

## Phase Goal

**As a** Storyteller, **I want to** walk the setup wizard, get a legal TB bag, and record who drew which role before night starts, **so that** I can reach a Night ready handoff without a paper setup sheet.

<objective>
Wave 0 vertical prep: install session/test stack and land failing Playwright + stub/home expectation updates so plan 02-02 can green the script→players slice (D-21).

Purpose: Unblock Zustand persist and domain Vitest without shipping wizard UI yet.
Output: Deps + lockfile; vitest.config.ts; RED e2e/setup-wizard.spec.ts; updated stubs/home E2E; VALIDATION Wave 0 notes.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-CONTEXT.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md
@.planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
@.planning/research/STACK.md

<interfaces>
UI-SPEC primary CTAs this plan asserts in E2E copy only: Continue setup, Next step, Add player, Difficulty heading.

No production UI files in this plan — SetupWizard lands in 02-02.
</interfaces>
</context>

## Artifacts this phase produces

(Plan 01 — Wave 0 deps + RED E2E)

- Deps: `zustand@^5.0.14`, `idb-keyval@^6.3.0`, `vitest@^4.1.10`; `vitest.config.ts`; `test:unit` script
- E2E: `e2e/setup-wizard.spec.ts` (RED); updated `e2e/stubs.spec.ts` + `e2e/home.spec.ts`
- VALIDATION Wave 0 install/config progress notes

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Wave 0 deps and Vitest config</name>
  <files>package.json, package-lock.json, vitest.config.ts, .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-RESEARCH.md (Standard Stack install, Package Legitimacy, Validation Architecture)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md (Wave 0 checklist)
    - package.json
  </read_first>
  <behavior>
    - package.json lists zustand, idb-keyval, vitest; scripts include test:unit
    - vitest.config.ts uses environment node and includes src/**/*.test.ts
  </behavior>
  <action>
    Install runtime deps zustand@^5.0.14 and idb-keyval@^6.3.0 and devDependency vitest@^4.1.10 per RESEARCH (legitimacy Approved — no human gate). Commit lockfile with package.json.

    Add vitest.config.ts with environment node and include src/**/*.test.ts. Add npm script test:unit that runs vitest run.

    Mark VALIDATION.md Wave 0 install/config rows in progress notes for deps + vitest config; leave domain test file rows and E2E file rows for Task 2 / later plans.
  </action>
  <acceptance_criteria>
    - package.json lists zustand, idb-keyval, and vitest
    - vitest.config.ts exists with environment node
    - package.json scripts include test:unit
    - CURSOR_DEV=true nix develop -c npx vitest --version exits 0
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npx vitest --version</automated>
  </verify>
  <done>Deps installed; Vitest configured; lockfile committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED wizard E2E + stub/home expectation rewrite</name>
  <files>e2e/setup-wizard.spec.ts, e2e/stubs.spec.ts, e2e/home.spec.ts, .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-setup-wizard-grimoire-capture/02-UI-SPEC.md (Script + Players copywriting)
    - .planning/phases/02-setup-wizard-grimoire-capture/02-VALIDATION.md
    - e2e/stubs.spec.ts
    - e2e/home.spec.ts
  </read_first>
  <behavior>
    - setup-wizard E2E: phone 390×844; Start setup → Continue setup → add five named players → Next step reaches Difficulty heading
    - stubs: /setup asserts Trouble Brewing + Continue setup + Back to home; /play stub unchanged
    - home smoke: after Start setup, expects Trouble Brewing / Continue setup / Back to home
  </behavior>
  <action>
    Create e2e/setup-wizard.spec.ts as the RED gate for the roster slice (D-21): viewport 390×844; navigate from home Start setup; assert script step copy from UI-SPEC; continue; add five players via Add player; fill unique names; click Next step; expect Difficulty heading visible. Do not import catalog JSON in the spec.

    Update e2e/stubs.spec.ts and e2e/home.spec.ts so /setup expectations match script-step UI-SPEC (Trouble Brewing, Continue setup, Back to home). Keep /play stub assertions.

    Note in VALIDATION.md that setup-wizard.spec + stub/home rewrites exist (still RED until plan 02-02).
  </action>
  <acceptance_criteria>
    - e2e/setup-wizard.spec.ts exists and references Continue setup and Difficulty
    - e2e/stubs.spec.ts /setup test no longer requires heading name Setup as the only setup assertion
    - CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts exits non-zero (RED) before plan 02-02 greens it
  </acceptance_criteria>
  <verify>
    <automated>CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts</automated>
  </verify>
  <done>Wizard E2E and stub/home updates committed; setup-wizard spec fails until plan 02-02 implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package-lock | Supply-chain trust for new deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs zustand/idb-keyval/vitest | high | mitigate | RESEARCH legitimacy Approved; pin versions from RESEARCH; commit lockfile |
</threat_model>

<verification>
CURSOR_DEV=true nix develop -c npx vitest --version
CURSOR_DEV=true nix develop -c npx playwright test e2e/setup-wizard.spec.ts
</verification>

<success_criteria>
- zustand, idb-keyval, vitest installed with test:unit
- RED setup-wizard E2E exists
- stub/home E2E expect script-step copy
</success_criteria>

<output>
Create `.planning/phases/02-setup-wizard-grimoire-capture/02-01-SUMMARY.md` when done
</output>
